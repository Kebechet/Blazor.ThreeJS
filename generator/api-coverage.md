# API coverage

Generated by `generator/emitter` from `generator/three-api.json` (`@types/three@0.185.3`).
Re-run `npm run emit` to refresh; `generator/api-coverage.json` carries the same data for the README table.

This file answers two questions and nothing else: **which of three.js's classes the generator can
produce**, and **what happens to every member it declares**. Anything not covered says why, in terms of
the specific obstacle rather than the word "unsupported".

## How a TypeScript type becomes a C# type

Every type reference in the snapshot resolves to exactly one of five outcomes. There is no sixth,
so nothing reaches generated code without either a mapping or a recorded reason.

| outcome | what it covers |
|---|---|
| C# primitive | `number` (`float` / `int`, see the numeric policy in `emitter-audit.md`), `boolean`, `string`, `void` |
| hand-written math type | `Vector3`, `Euler`, `Quaternion`, `Matrix4`, `Color` — encoded inline on the wire, never regenerated |
| generated wrapper class | any emitted class, passed by handle |
| generated enum | a type alias unioning `typeof` of numeric constants, or a numeric TypeScript `enum` |
| **skipped** | everything else, with the obstacle named |

Four rules are worth stating explicitly because they are judgement calls, not mechanics:

1. **`ColorRepresentation` maps to `Color`.** three.js accepts `Color | string | number` wherever a colour
   is taken; the mirror exposes only `Color`, which reaches the browser as a real `THREE.Color` and covers
   the hex form through `Color.FromHex`. This is a **narrowing**: the CSS-string spelling is not exposed.
2. **Type parameters are erased to their default, failing that their constraint.** The C# object model is
   non-generic by design, so `Mesh<TGeometry = BufferGeometry>` maps as if the parameter were written out.
3. **An optional parameter whose type does not map is dropped, and everything after it with it.** Calling
   the JavaScript constructor with fewer arguments is exactly what three.js is built for, so the emitted
   class is a faithful subset rather than a guess. A **required** parameter that does not map blocks the
   whole class instead. Every dropped parameter is listed below — this is the one rule that narrows the
   public API relative to three.js.
4. **`src/math/**` is out of the generated surface.** Math values are by-value types encoded inline, not
   handle-backed objects; five are hand-written and the rest would need a representation decision first.

## Classes

| status | classes |
|---|---|
| emittable | 190 |
| deliberately out of the mirrored surface | 59 |
| blocked | 60 |
| **total** | **309** |

### Out of the mirrored surface, by rule

| obstacle | classes | example reason |
|---|---|---|
| `UnwrappedClass` | 32 | renderer internals under `src/renderers/webgl/**`; no consumer instantiates them and emitting them would inflate the coverage table |
| `MathValueType` | 27 | a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |

The consumer-facing renderer types are checked against the exclusion rather than special-cased:

- `WebGL3DRenderTarget`: emittable
- `WebGLArrayRenderTarget`: emittable
- `WebGLCubeRenderTarget`: emittable
- `WebGLRenderTarget`: emittable
- `WebGLRenderer`: emittable

### Blocked, by cause

| obstacle | classes | example reason |
|---|---|---|
| `UnmappedUnion` | 18 | required parameter 'mixer' cannot be mapped: `AnimationMixer` is not an emitted class: required parameter 'root' cannot be mapped: `Object3D | AnimationObjectGroup` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `AbstractClass` | 13 | the class is abstract, so it has no constructor to mirror |
| `DomOrLibType` | 9 | required parameter 'times' cannot be mapped: `ArrayLike<number>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `UnmappedTypeAlias` | 6 | required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `CollectionType` | 3 | required parameter 'mipmaps' cannot be mapped: `CompressedTextureMipmap[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `DuplicateClassName` | 3 | another class named `PMREMGenerator` is declared in `src/extras/PMREMGenerator.d.ts`, and a C# namespace holds one type of a given name |
| `MathValueType` | 2 | required parameter 'box' cannot be mapped: `Box3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `NotExported` | 2 | the class is never exported, so it is not reachable on the `THREE` namespace the applier looks names up on |
| `ConstructorOverloads` | 1 | 2 constructor overloads; C# overload emission is not implemented |
| `NodeStackType` | 1 | required parameter 'builder' cannot be mapped: `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `UnerasableTypeParameter` | 1 | required parameter 'data' cannot be mapped: type parameter `TData` has neither a default nor a constraint, so erasing it leaves nothing to map to |
| `UntypedValue` | 1 | required parameter 'value' cannot be mapped: `any` carries no type information a C# signature could express |

<details><summary>Every blocked class</summary>

| class | why | file |
|---|---|---|
| `AnimationAction` | required parameter 'mixer' cannot be mapped: `AnimationMixer` is not an emitted class: required parameter 'root' cannot be mapped: `Object3D | AnimationObjectGroup` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently | `src/animation/AnimationAction.d.ts` |
| `AnimationMixer` | required parameter 'root' cannot be mapped: `Object3D | AnimationObjectGroup` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently | `src/animation/AnimationMixer.d.ts` |
| `Backend` | the class is abstract, so it has no constructor to mirror | `src/renderers/common/Backend.d.ts` |
| `BooleanKeyframeTrack` | required parameter 'times' cannot be mapped: `ArrayLike<number>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one | `src/animation/tracks/BooleanKeyframeTrack.d.ts` |
| `Box3Helper` | required parameter 'box' cannot be mapped: `Box3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one | `src/helpers/Box3Helper.d.ts` |
| `BufferAttribute` | required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses | `src/core/BufferAttribute.d.ts` |
| `CanvasTarget` | required parameter 'domElement' cannot be mapped: `HTMLCanvasElement | OffscreenCanvas` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently | `src/renderers/common/CanvasTarget.d.ts` |
| `ColorKeyframeTrack` | required parameter 'times' cannot be mapped: `ArrayLike<number>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one | `src/animation/tracks/ColorKeyframeTrack.d.ts` |
| `Composite` | the class is never exported, so it is not reachable on the `THREE` namespace the applier looks names up on | `src/animation/PropertyBinding.d.ts` |
| `CompressedArrayTexture` | required parameter 'mipmaps' cannot be mapped: `CompressedTextureMipmap[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding | `src/textures/CompressedArrayTexture.d.ts` |
| `CompressedCubeTexture` | required parameter 'images' cannot be mapped: `CompressedTextureImageData[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding | `src/textures/CompressedCubeTexture.d.ts` |
| `CompressedTexture` | required parameter 'mipmaps' cannot be mapped: `CompressedTextureMipmap[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding | `src/textures/CompressedTexture.d.ts` |
| `Controls` | the class is abstract, so it has no constructor to mirror | `src/extras/Controls.d.ts` |
| `CubeCamera` | required parameter 'renderTarget' cannot be mapped: `WebGLCubeRenderTarget | CubeRenderTarget` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently | `src/cameras/CubeCamera.d.ts` |
| `Curve` | the class is abstract, so it has no constructor to mirror | `src/extras/core/Curve.d.ts` |
| `DataTextureLoader` | the class is abstract, so it has no constructor to mirror | `src/loaders/DataTextureLoader.d.ts` |
| `Float16BufferAttribute` | required parameter 'array' cannot be mapped: `Iterable<number> | ArrayLike<number> | ArrayBuffer | number` unions 4 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently | `src/core/BufferAttribute.d.ts` |
| `Float32BufferAttribute` | required parameter 'array' cannot be mapped: `Iterable<number> | ArrayLike<number> | ArrayBuffer | number` unions 4 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently | `src/core/BufferAttribute.d.ts` |
| `GLBufferAttribute` | required parameter 'buffer' cannot be mapped: `WebGLBuffer` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one | `src/core/GLBufferAttribute.d.ts` |
| `GLSLNodeBuilder` | required parameter 'renderer' cannot be mapped: `Renderer` is not an emitted class: required parameter 'backend' cannot be mapped: `Backend` is not an emitted class: the class is abstract, so it has no constructor to mirror | `src/renderers/webgl-fallback/nodes/GLSLNodeBuilder.d.ts` |
| `IndirectStorageBufferAttribute` | required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses | `src/renderers/common/IndirectStorageBufferAttribute.d.ts` |
| `InstancedBufferAttribute` | required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses | `src/core/InstancedBufferAttribute.d.ts` |
| `InstancedInterleavedBuffer` | required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses | `src/core/InstancedInterleavedBuffer.d.ts` |
| `InstancedMesh` | required parameter 'material' cannot be mapped: `Material | Material[]` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently | `src/objects/InstancedMesh.d.ts` |
| `Int16BufferAttribute` | required parameter 'array' cannot be mapped: `Iterable<number> | ArrayLike<number> | ArrayBuffer | number` unions 4 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently | `src/core/BufferAttribute.d.ts` |
| `Int32BufferAttribute` | required parameter 'array' cannot be mapped: `Iterable<number> | ArrayLike<number> | ArrayBuffer | number` unions 4 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently | `src/core/BufferAttribute.d.ts` |
| `Int8BufferAttribute` | required parameter 'array' cannot be mapped: `Iterable<number> | ArrayLike<number> | ArrayBuffer | number` unions 4 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently | `src/core/BufferAttribute.d.ts` |
| `InterleavedBuffer` | required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses | `src/core/InterleavedBuffer.d.ts` |
| `InterleavedBufferAttribute` | required parameter 'interleavedBuffer' cannot be mapped: `InterleavedBuffer` is not an emitted class: required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses | `src/core/InterleavedBufferAttribute.d.ts` |
| `KeyframeTrack` | required parameter 'times' cannot be mapped: `ArrayLike<number>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one | `src/animation/KeyframeTrack.d.ts` |
| `Light` | the class is abstract, so it has no constructor to mirror | `src/lights/Light.d.ts` |
| `LightShadow` | the class is abstract, so it has no constructor to mirror | `src/lights/LightShadow.d.ts` |
| `NodeMaterialObserver` | required parameter 'builder' cannot be mapped: `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface | `src/materials/nodes/manager/NodeMaterialObserver.d.ts` |
| `NumberKeyframeTrack` | required parameter 'times' cannot be mapped: `ArrayLike<number>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one | `src/animation/tracks/NumberKeyframeTrack.d.ts` |
| `PMREMGenerator` | another class named `PMREMGenerator` is declared in `src/extras/PMREMGenerator.d.ts`, and a C# namespace holds one type of a given name | `src/renderers/common/extras/PMREMGenerator.d.ts` |
| `PlaneHelper` | required parameter 'plane' cannot be mapped: `Plane` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one | `src/helpers/PlaneHelper.d.ts` |
| `PostProcessing` | required parameter 'renderer' cannot be mapped: `Renderer` is not an emitted class: required parameter 'backend' cannot be mapped: `Backend` is not an emitted class: the class is abstract, so it has no constructor to mirror | `src/renderers/common/PostProcessing.d.ts` |
| `PropertyBinding` | required parameter 'rootNode' cannot be mapped: `Object3D | Skeleton` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently | `src/animation/PropertyBinding.d.ts` |
| `PropertyMixer` | required parameter 'binding' cannot be mapped: `PropertyBinding` is not an emitted class: required parameter 'rootNode' cannot be mapped: `Object3D | Skeleton` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently | `src/animation/PropertyMixer.d.ts` |
| `QuaternionKeyframeTrack` | required parameter 'times' cannot be mapped: `ArrayLike<number>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one | `src/animation/tracks/QuaternionKeyframeTrack.d.ts` |
| `RenderPipeline` | required parameter 'renderer' cannot be mapped: `Renderer` is not an emitted class: required parameter 'backend' cannot be mapped: `Backend` is not an emitted class: the class is abstract, so it has no constructor to mirror | `src/renderers/common/RenderPipeline.d.ts` |
| `Renderer` | required parameter 'backend' cannot be mapped: `Backend` is not an emitted class: the class is abstract, so it has no constructor to mirror | `src/renderers/common/Renderer.d.ts` |
| `SSSLightingModel` | the class is never exported, so it is not reachable on the `THREE` namespace the applier looks names up on | `src/materials/nodes/MeshSSSNodeMaterial.d.ts` |
| `Source` | required parameter 'data' cannot be mapped: type parameter `TData` has neither a default nor a constraint, so erasing it leaves nothing to map to | `src/textures/Source.d.ts` |
| `Storage3DTexture` | another class named `Storage3DTexture` is declared in `src/renderers/common/Storage3DTexture.d.ts`, and a C# namespace holds one type of a given name | `src/renderers/common/StorageArrayTexture.d.ts` |
| `StorageBufferAttribute` | required parameter 'array' cannot be mapped: `TypedArray | number` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently | `src/renderers/common/StorageBufferAttribute.d.ts` |
| `StorageInstancedBufferAttribute` | required parameter 'array' cannot be mapped: `TypedArray | number` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently | `src/renderers/common/StorageInstancedBufferAttribute.d.ts` |
| `StringKeyframeTrack` | required parameter 'times' cannot be mapped: `ArrayLike<number>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one | `src/animation/tracks/StringKeyframeTrack.d.ts` |
| `Texture` | 2 constructor overloads; C# overload emission is not implemented | `src/textures/Texture.d.ts` |
| `TimestampQueryPool` | the class is abstract, so it has no constructor to mirror | `src/renderers/common/TimestampQueryPool.d.ts` |
| `Uint16BufferAttribute` | required parameter 'array' cannot be mapped: `Iterable<number> | ArrayLike<number> | ArrayBuffer | number` unions 4 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently | `src/core/BufferAttribute.d.ts` |
| `Uint32BufferAttribute` | required parameter 'array' cannot be mapped: `Iterable<number> | ArrayLike<number> | ArrayBuffer | number` unions 4 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently | `src/core/BufferAttribute.d.ts` |
| `Uint8BufferAttribute` | required parameter 'array' cannot be mapped: `Iterable<number> | ArrayLike<number> | ArrayBuffer | number` unions 4 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently | `src/core/BufferAttribute.d.ts` |
| `Uint8ClampedBufferAttribute` | required parameter 'array' cannot be mapped: `Iterable<number> | ArrayLike<number> | ArrayBuffer | number` unions 4 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently | `src/core/BufferAttribute.d.ts` |
| `Uniform` | required parameter 'value' cannot be mapped: `any` carries no type information a C# signature could express | `src/core/Uniform.d.ts` |
| `VectorKeyframeTrack` | required parameter 'times' cannot be mapped: `ArrayLike<number>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one | `src/animation/tracks/VectorKeyframeTrack.d.ts` |
| `VideoTexture` | required parameter 'video' cannot be mapped: `HTMLVideoElement` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one | `src/textures/VideoTexture.d.ts` |
| `WGSLNodeBuilder` | required parameter 'renderer' cannot be mapped: `Renderer` is not an emitted class: required parameter 'backend' cannot be mapped: `Backend` is not an emitted class: the class is abstract, so it has no constructor to mirror | `src/renderers/webgpu/nodes/WGSLNodeBuilder.d.ts` |
| `WebGPURenderer` | another class named `WebGPURenderer` is declared in `src/renderers/webgpu/WebGPURenderer.Nodes.d.ts`, and a C# namespace holds one type of a given name | `src/renderers/webgpu/WebGPURenderer.d.ts` |
| `XRManager` | required parameter 'renderer' cannot be mapped: `Renderer` is not an emitted class: required parameter 'backend' cannot be mapped: `Backend` is not an emitted class: the class is abstract, so it has no constructor to mirror | `src/renderers/common/XRManager.d.ts` |

</details>

<details><summary>Every emittable class</summary>

| class | constructor parameters | dropped | file |
|---|---|---|---|
| `AmbientLight` | 2 | — | `src/lights/AmbientLight.d.ts` |
| `AnimationClip` | 2 | `tracks`, `blendMode` | `src/animation/AnimationClip.d.ts` |
| `AnimationLoader` | 1 | — | `src/loaders/AnimationLoader.d.ts` |
| `AnimationObjectGroup` | 0 | `args` | `src/animation/AnimationObjectGroup.d.ts` |
| `AnimationUtils` | 0 | — | `src/animation/AnimationUtils.d.ts` |
| `ArcCurve` | 6 | — | `src/extras/curves/ArcCurve.d.ts` |
| `ArrayCamera` | 0 | `array` | `src/cameras/ArrayCamera.d.ts` |
| `ArrowHelper` | 6 | — | `src/helpers/ArrowHelper.d.ts` |
| `Audio` | 1 | — | `src/audio/Audio.d.ts` |
| `AudioAnalyser` | 2 | — | `src/audio/AudioAnalyser.d.ts` |
| `AudioContext` | 0 | — | `src/audio/AudioContext.d.ts` |
| `AudioListener` | 0 | — | `src/audio/AudioListener.d.ts` |
| `AudioLoader` | 1 | — | `src/loaders/AudioLoader.d.ts` |
| `AxesHelper` | 1 | — | `src/helpers/AxesHelper.d.ts` |
| `BasicNodeLibrary` | 0 | — | `src/renderers/webgpu/nodes/BasicNodeLibrary.d.ts` |
| `BatchedMesh` | 4 | — | `src/objects/BatchedMesh.d.ts` |
| `BlendMode` | 1 | — | `src/renderers/common/BlendMode.d.ts` |
| `Bone` | 0 | — | `src/objects/Bone.d.ts` |
| `BoxGeometry` | 6 | — | `src/geometries/BoxGeometry.d.ts` |
| `BoxHelper` | 2 | — | `src/helpers/BoxHelper.d.ts` |
| `BufferGeometry` | 0 | — | `src/core/BufferGeometry.d.ts` |
| `BufferGeometryLoader` | 1 | — | `src/loaders/BufferGeometryLoader.d.ts` |
| `BundleGroup` | 0 | — | `src/renderers/common/BundleGroup.d.ts` |
| `Camera` | 0 | — | `src/cameras/Camera.d.ts` |
| `CameraHelper` | 1 | — | `src/helpers/CameraHelper.d.ts` |
| `CanvasTexture` | 0 | `canvas`, `mapping`, `wrapS`, `wrapT`, `magFilter`, `minFilter`, `format`, `type`, `anisotropy` | `src/textures/CanvasTexture.d.ts` |
| `CapsuleGeometry` | 5 | — | `src/geometries/CapsuleGeometry.d.ts` |
| `CatmullRomCurve3` | 0 | `points`, `closed`, `curveType`, `tension` | `src/extras/curves/CatmullRomCurve3.d.ts` |
| `CircleGeometry` | 4 | — | `src/geometries/CircleGeometry.d.ts` |
| `ClippingContext` | 1 | — | `src/renderers/common/ClippingContext.d.ts` |
| `ClippingGroup` | 0 | — | `src/objects/ClippingGroup.d.ts` |
| `Clock` | 1 | — | `src/core/Clock.d.ts` |
| `CompressedTextureLoader` | 1 | — | `src/loaders/CompressedTextureLoader.d.ts` |
| `ConeGeometry` | 7 | — | `src/geometries/ConeGeometry.d.ts` |
| `CubeDepthTexture` | 9 | — | `src/textures/CubeDepthTexture.d.ts` |
| `CubeRenderTarget` | 1 | `options` | `src/renderers/common/CubeRenderTarget.d.ts` |
| `CubeTexture` | 0 | `images`, `mapping`, `wrapS`, `wrapT`, `magFilter`, `minFilter`, `format`, `type`, `anisotropy`, `colorSpace` | `src/textures/CubeTexture.d.ts` |
| `CubeTextureLoader` | 1 | — | `src/loaders/CubeTextureLoader.d.ts` |
| `CubicBezierCurve` | 0 | `v0`, `v1`, `v2`, `v3` | `src/extras/curves/CubicBezierCurve.d.ts` |
| `CubicBezierCurve3` | 4 | — | `src/extras/curves/CubicBezierCurve3.d.ts` |
| `CurvePath` | 0 | — | `src/extras/core/CurvePath.d.ts` |
| `CylinderGeometry` | 8 | — | `src/geometries/CylinderGeometry.d.ts` |
| `Data3DTexture` | 0 | `data`, `width`, `height`, `depth` | `src/textures/Data3DTexture.d.ts` |
| `DataArrayTexture` | 0 | `data`, `width`, `height`, `depth` | `src/textures/DataArrayTexture.d.ts` |
| `DataTexture` | 0 | `data`, `width`, `height`, `format`, `type`, `mapping`, `wrapS`, `wrapT`, `magFilter`, `minFilter`, `anisotropy`, `colorSpace` | `src/textures/DataTexture.d.ts` |
| `DataUtils` | 0 | — | `src/extras/DataUtils.d.ts` |
| `DepthTexture` | 11 | — | `src/textures/DepthTexture.d.ts` |
| `DirectionalLight` | 2 | — | `src/lights/DirectionalLight.d.ts` |
| `DirectionalLightHelper` | 3 | — | `src/helpers/DirectionalLightHelper.d.ts` |
| `DirectionalLightShadow` | 0 | — | `src/lights/DirectionalLightShadow.d.ts` |
| `DodecahedronGeometry` | 2 | — | `src/geometries/DodecahedronGeometry.d.ts` |
| `Earcut` | 0 | — | `src/extras/Earcut.d.ts` |
| `EdgesGeometry` | 2 | — | `src/geometries/EdgesGeometry.d.ts` |
| `EllipseCurve` | 8 | — | `src/extras/curves/EllipseCurve.d.ts` |
| `EventDispatcher` | 0 | — | `src/core/EventDispatcher.d.ts` |
| `ExternalTexture` | 0 | `sourceTexture` | `src/textures/ExternalTexture.d.ts` |
| `ExtrudeGeometry` | 0 | `shapes`, `options` | `src/geometries/ExtrudeGeometry.d.ts` |
| `FileLoader` | 1 | — | `src/loaders/FileLoader.d.ts` |
| `Fog` | 3 | — | `src/scenes/Fog.d.ts` |
| `FogExp2` | 2 | — | `src/scenes/FogExp2.d.ts` |
| `FramebufferTexture` | 2 | — | `src/textures/FramebufferTexture.d.ts` |
| `GridHelper` | 4 | — | `src/helpers/GridHelper.d.ts` |
| `Group` | 0 | — | `src/objects/Group.d.ts` |
| `HTMLTexture` | 0 | `image`, `mapping`, `wrapS`, `wrapT`, `magFilter`, `minFilter`, `format`, `type`, `anisotropy` | `src/textures/HTMLTexture.d.ts` |
| `HemisphereLight` | 3 | — | `src/lights/HemisphereLight.d.ts` |
| `HemisphereLightHelper` | 3 | — | `src/helpers/HemisphereLightHelper.d.ts` |
| `IESSpotLight` | 0 | — | `src/lights/webgpu/IESSpotLight.d.ts` |
| `IcosahedronGeometry` | 2 | — | `src/geometries/IcosahedronGeometry.d.ts` |
| `ImageBitmapLoader` | 1 | — | `src/loaders/ImageBitmapLoader.d.ts` |
| `ImageLoader` | 1 | — | `src/loaders/ImageLoader.d.ts` |
| `ImageUtils` | 0 | — | `src/extras/ImageUtils.d.ts` |
| `Info` | 0 | — | `src/renderers/common/Info.d.ts` |
| `InspectorBase` | 0 | — | `src/renderers/common/InspectorBase.d.ts` |
| `InstancedBufferGeometry` | 0 | — | `src/core/InstancedBufferGeometry.d.ts` |
| `LOD` | 0 | — | `src/objects/LOD.d.ts` |
| `LatheGeometry` | 0 | `points`, `segments`, `phiStart`, `phiLength` | `src/geometries/LatheGeometry.d.ts` |
| `Layers` | 0 | — | `src/core/Layers.d.ts` |
| `LightProbe` | 0 | `sh`, `intensity` | `src/lights/LightProbe.d.ts` |
| `Lighting` | 0 | — | `src/renderers/common/Lighting.d.ts` |
| `Line` | 1 | `material` | `src/objects/Line.d.ts` |
| `Line2NodeMaterial` | 0 | `parameters` | `src/materials/nodes/Line2NodeMaterial.d.ts` |
| `LineBasicMaterial` | 0 | `parameters` | `src/materials/LineBasicMaterial.d.ts` |
| `LineBasicNodeMaterial` | 0 | `parameters` | `src/materials/nodes/LineBasicNodeMaterial.d.ts` |
| `LineCurve` | 0 | `v1`, `v2` | `src/extras/curves/LineCurve.d.ts` |
| `LineCurve3` | 2 | — | `src/extras/curves/LineCurve3.d.ts` |
| `LineDashedMaterial` | 0 | `parameters` | `src/materials/LineDashedMaterial.d.ts` |
| `LineDashedNodeMaterial` | 0 | `parameters` | `src/materials/nodes/LineDashedNodeMaterial.d.ts` |
| `LineLoop` | 1 | `material` | `src/objects/LineLoop.d.ts` |
| `LineSegments` | 1 | `material` | `src/objects/LineSegments.d.ts` |
| `Loader` | 1 | — | `src/loaders/Loader.d.ts` |
| `LoaderUtils` | 0 | — | `src/loaders/LoaderUtils.d.ts` |
| `LoadingManager` | 0 | `onLoad`, `onProgress`, `onError` | `src/loaders/LoadingManager.d.ts` |
| `Material` | 0 | — | `src/materials/Material.d.ts` |
| `MaterialLoader` | 1 | — | `src/loaders/MaterialLoader.d.ts` |
| `Mesh` | 1 | `material` | `src/objects/Mesh.d.ts` |
| `MeshBasicMaterial` | 0 | `parameters` | `src/materials/MeshBasicMaterial.d.ts` |
| `MeshBasicNodeMaterial` | 0 | `parameters` | `src/materials/nodes/MeshBasicNodeMaterial.d.ts` |
| `MeshDepthMaterial` | 0 | `parameters` | `src/materials/MeshDepthMaterial.d.ts` |
| `MeshDistanceMaterial` | 0 | `parameters` | `src/materials/MeshDistanceMaterial.d.ts` |
| `MeshLambertMaterial` | 0 | `parameters` | `src/materials/MeshLambertMaterial.d.ts` |
| `MeshLambertNodeMaterial` | 0 | `parameters` | `src/materials/nodes/MeshLambertNodeMaterial.d.ts` |
| `MeshMatcapMaterial` | 0 | `parameters` | `src/materials/MeshMatcapMaterial.d.ts` |
| `MeshMatcapNodeMaterial` | 0 | `parameters` | `src/materials/nodes/MeshMatcapNodeMaterial.d.ts` |
| `MeshNormalMaterial` | 0 | `parameters` | `src/materials/MeshNormalMaterial.d.ts` |
| `MeshNormalNodeMaterial` | 0 | `parameters` | `src/materials/nodes/MeshNormalNodeMaterial.d.ts` |
| `MeshPhongMaterial` | 0 | `parameters` | `src/materials/MeshPhongMaterial.d.ts` |
| `MeshPhongNodeMaterial` | 0 | `parameters` | `src/materials/nodes/MeshPhongNodeMaterial.d.ts` |
| `MeshPhysicalMaterial` | 0 | `parameters` | `src/materials/MeshPhysicalMaterial.d.ts` |
| `MeshPhysicalNodeMaterial` | 0 | `parameters` | `src/materials/nodes/MeshPhysicalNodeMaterial.d.ts` |
| `MeshSSSNodeMaterial` | 0 | `parameters` | `src/materials/nodes/MeshSSSNodeMaterial.d.ts` |
| `MeshStandardMaterial` | 0 | `parameters` | `src/materials/MeshStandardMaterial.d.ts` |
| `MeshStandardNodeMaterial` | 0 | `parameters` | `src/materials/nodes/MeshStandardNodeMaterial.d.ts` |
| `MeshToonMaterial` | 0 | `parameters` | `src/materials/MeshToonMaterial.d.ts` |
| `MeshToonNodeMaterial` | 0 | `parameters` | `src/materials/nodes/MeshToonNodeMaterial.d.ts` |
| `NodeLibrary` | 0 | — | `src/renderers/common/nodes/NodeLibrary.d.ts` |
| `NodeLoader` | 1 | — | `src/loaders/nodes/NodeLoader.d.ts` |
| `NodeMaterial` | 0 | — | `src/materials/nodes/NodeMaterial.d.ts` |
| `NodeMaterialLoader` | 0 | — | `src/loaders/nodes/NodeMaterialLoader.d.ts` |
| `NodeObjectLoader` | 1 | — | `src/loaders/nodes/NodeObjectLoader.d.ts` |
| `Object3D` | 0 | — | `src/core/Object3D.d.ts` |
| `ObjectLoader` | 1 | — | `src/loaders/ObjectLoader.d.ts` |
| `OctahedronGeometry` | 2 | — | `src/geometries/OctahedronGeometry.d.ts` |
| `OrthographicCamera` | 6 | — | `src/cameras/OrthographicCamera.d.ts` |
| `PMREMGenerator` | 1 | — | `src/extras/PMREMGenerator.d.ts` |
| `Path` | 0 | `points` | `src/extras/core/Path.d.ts` |
| `PerspectiveCamera` | 4 | — | `src/cameras/PerspectiveCamera.d.ts` |
| `PlaneGeometry` | 4 | — | `src/geometries/PlaneGeometry.d.ts` |
| `PointLight` | 4 | — | `src/lights/PointLight.d.ts` |
| `PointLightHelper` | 3 | — | `src/helpers/PointLightHelper.d.ts` |
| `PointLightShadow` | 0 | — | `src/lights/PointLightShadow.d.ts` |
| `Points` | 1 | `material` | `src/objects/Points.d.ts` |
| `PointsMaterial` | 0 | `parameters` | `src/materials/PointsMaterial.d.ts` |
| `PointsNodeMaterial` | 0 | `parameters` | `src/materials/nodes/PointsNodeMaterial.d.ts` |
| `PolarGridHelper` | 6 | — | `src/helpers/PolarGridHelper.d.ts` |
| `PolyhedronGeometry` | 0 | `vertices`, `indices`, `radius`, `detail` | `src/geometries/PolyhedronGeometry.d.ts` |
| `PositionalAudio` | 0 | — | `src/audio/PositionalAudio.d.ts` |
| `ProjectorLight` | 0 | — | `src/lights/webgpu/ProjectorLight.d.ts` |
| `QuadMesh` | 1 | — | `src/renderers/common/QuadMesh.d.ts` |
| `QuadraticBezierCurve` | 0 | `v0`, `v1`, `v2` | `src/extras/curves/QuadraticBezierCurve.d.ts` |
| `QuadraticBezierCurve3` | 3 | — | `src/extras/curves/QuadraticBezierCurve3.d.ts` |
| `RawShaderMaterial` | 0 | — | `src/materials/RawShaderMaterial.d.ts` |
| `Raycaster` | 4 | — | `src/core/Raycaster.d.ts` |
| `ReadbackBuffer` | 1 | — | `src/renderers/common/ReadbackBuffer.d.ts` |
| `RectAreaLight` | 4 | — | `src/lights/RectAreaLight.d.ts` |
| `RenderTarget` | 2 | `options` | `src/core/RenderTarget.d.ts` |
| `RenderTarget3D` | 3 | `options` | `src/core/RenderTarget3D.d.ts` |
| `RingGeometry` | 6 | — | `src/geometries/RingGeometry.d.ts` |
| `Scene` | 0 | — | `src/scenes/Scene.d.ts` |
| `ShaderMaterial` | 0 | `parameters` | `src/materials/ShaderMaterial.d.ts` |
| `ShadowMaterial` | 0 | `parameters` | `src/materials/ShadowMaterial.d.ts` |
| `ShadowNodeMaterial` | 0 | `parameters` | `src/materials/nodes/ShadowNodeMaterial.d.ts` |
| `Shape` | 0 | `points` | `src/extras/core/Shape.d.ts` |
| `ShapeGeometry` | 0 | `shapes`, `curveSegments` | `src/geometries/ShapeGeometry.d.ts` |
| `ShapePath` | 0 | — | `src/extras/core/ShapePath.d.ts` |
| `ShapeUtils` | 0 | — | `src/extras/ShapeUtils.d.ts` |
| `Skeleton` | 0 | `bones`, `boneInverses` | `src/objects/Skeleton.d.ts` |
| `SkeletonHelper` | 1 | — | `src/helpers/SkeletonHelper.d.ts` |
| `SkinnedMesh` | 1 | `material`, `useVertexTexture` | `src/objects/SkinnedMesh.d.ts` |
| `SourceJSON` | 0 | — | `src/textures/Source.d.ts` |
| `SphereGeometry` | 7 | — | `src/geometries/SphereGeometry.d.ts` |
| `SplineCurve` | 0 | `points` | `src/extras/curves/SplineCurve.d.ts` |
| `SpotLight` | 6 | — | `src/lights/SpotLight.d.ts` |
| `SpotLightHelper` | 2 | — | `src/helpers/SpotLightHelper.d.ts` |
| `SpotLightShadow` | 0 | — | `src/lights/SpotLightShadow.d.ts` |
| `Sprite` | 1 | — | `src/objects/Sprite.d.ts` |
| `SpriteMaterial` | 0 | `parameters` | `src/materials/SpriteMaterial.d.ts` |
| `SpriteNodeMaterial` | 0 | `parameters` | `src/materials/nodes/SpriteNodeMaterial.d.ts` |
| `StandardNodeLibrary` | 0 | — | `src/renderers/webgpu/nodes/StandardNodeLibrary.d.ts` |
| `StereoCamera` | 0 | — | `src/cameras/StereoCamera.d.ts` |
| `Storage3DTexture` | 3 | — | `src/renderers/common/Storage3DTexture.d.ts` |
| `StorageTexture` | 2 | — | `src/renderers/common/StorageTexture.d.ts` |
| `TetrahedronGeometry` | 2 | — | `src/geometries/TetrahedronGeometry.d.ts` |
| `TextureLoader` | 1 | — | `src/loaders/TextureLoader.d.ts` |
| `Timer` | 0 | — | `src/core/Timer.d.ts` |
| `TorusGeometry` | 7 | — | `src/geometries/TorusGeometry.d.ts` |
| `TorusKnotGeometry` | 6 | — | `src/geometries/TorusKnotGeometry.d.ts` |
| `TubeGeometry` | 0 | `path`, `tubularSegments`, `radius`, `radialSegments`, `closed` | `src/geometries/TubeGeometry.d.ts` |
| `UniformsGroup` | 0 | — | `src/core/UniformsGroup.d.ts` |
| `VideoFrameTexture` | 8 | — | `src/textures/VideoFrameTexture.d.ts` |
| `VolumeNodeMaterial` | 0 | `parameters` | `src/materials/nodes/VolumeNodeMaterial.d.ts` |
| `WebGL3DRenderTarget` | 3 | `options` | `src/renderers/WebGL3DRenderTarget.d.ts` |
| `WebGLArrayRenderTarget` | 3 | `options` | `src/renderers/WebGLArrayRenderTarget.d.ts` |
| `WebGLBackend` | 0 | `parameters` | `src/renderers/webgl-fallback/WebGLBackend.d.ts` |
| `WebGLCapabilities` | 1 | — | `src/renderers/webgl-fallback/utils/WebGLCapabilities.d.ts` |
| `WebGLCubeRenderTarget` | 1 | `options` | `src/renderers/WebGLCubeRenderTarget.d.ts` |
| `WebGLRenderTarget` | 2 | `options` | `src/renderers/WebGLRenderTarget.d.ts` |
| `WebGLRenderer` | 0 | `parameters` | `src/renderers/WebGLRenderer.d.ts` |
| `WebGPUBackend` | 0 | `parameters` | `src/renderers/webgpu/WebGPUBackend.d.ts` |
| `WebGPURenderer` | 0 | `parameters` | `src/renderers/webgpu/WebGPURenderer.Nodes.d.ts` |
| `WireframeGeometry` | 1 | — | `src/geometries/WireframeGeometry.d.ts` |

</details>

### Emittable, but their base is a `sealed` hand-written class

Plan 1 sealed the hand-written classes because only leaves were needed. three.js subclasses several
of them, so these map cleanly but cannot compile alongside the current hand-written types. Unsealing
them is a public-API change rather than a mapping decision.

| class | sealed base |
|---|---|
| `ArrayCamera` | `PerspectiveCamera` |
| `BatchedMesh` | `Mesh` |
| `BundleGroup` | `Group` |
| `ClippingGroup` | `Group` |
| `MeshPhysicalMaterial` | `MeshStandardMaterial` |
| `PointLightHelper` | `Mesh` |
| `QuadMesh` | `Mesh` |
| `SkinnedMesh` | `Mesh` |

## Members

Every property and method of every class in the IR, in one of four buckets. Classification is
independent of whether the declaring class is emitted, so the same rows answer both "how much of
three.js is reachable at all" and "how much of what we mirror is state".

| bucket | what it means | all classes | emittable classes |
|---|---|---|---|
| MirroredState | state C# holds and writes through on change | 477 | 268 |
| Command | a method recorded as a call op, returning nothing or `this` | 644 | 246 |
| AsyncQuery | a method whose result the caller needs back | 184 | 56 |
| Skipped | not mirrored; see the skip list below | 1290 | 667 |
| **total** | | **2595** | **1237** |

⚠️ **No async query is emittable yet.** The wire format has five op kinds — create, set, call, add,
remove, dispose — and none of them reads a value back. Every member in that bucket is classified and
waiting on a read op, not on a mapping.

⚠️ **31 methods declare more than one overload, and only the first is classified.** Each stands
for several C# overloads; the classification says what the first signature is, not how many methods a
full run would emit.

### Members merged in from module augmentations

A class's real member set is its own declaration plus every `declare module` block targeting it.
15 augmentations exist and contribute 5 members, which are classified here rather
than silently dropped.

| class | member | bucket | why |
|---|---|---|---|
| `Object3D` | `count` | MirroredState | mapped |
| `Object3D` | `occlusionTest` | MirroredState | mapped |
| `Scene` | `environmentNode` | Skipped | `Node<"vec3">` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `Scene` | `backgroundNode` | Skipped | `Node` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `Scene` | `fogNode` | Skipped | `Node` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |

## Skip list

Every member the mirror does not express, grouped by the obstacle. This is the "not covered" half of
the README's coverage table.

| obstacle | members | what it is |
|---|---|---|
| `OptionsInterface` | 161 | a structural interface — an options bag or an event map — with no C# type to be |
| `MathValueType` | 153 | a `src/math/**` value type beyond the five that are hand-written |
| `ReadOnlyWithoutReadChannel` | 117 | read-only in three.js, and the wire format has no read op |
| `UnmappedUnion` | 117 | a union of several real alternatives, which one C# parameter cannot express |
| `DomOrLibType` | 101 | a TypeScript lib or DOM type; C# holds no browser object and the wire has no encoding for one |
| `CallbackType` | 93 | a JavaScript callback; the wire format carries ops in one direction only |
| `NotInstanceApi` | 91 | static, non-public or `@internal` — not part of the mirrored instance API |
| `CollectionType` | 69 | an array or tuple; `ThreeValue.Encode` has no array arm |
| `LiteralType` | 67 | a literal type — three.js's `isMesh`-style runtime type tags |
| `UnmappedTypeAlias` | 61 | a type alias that is neither a constant group nor a rename of a mapped type |
| `AnonymousObjectType` | 58 | an anonymous object literal type with no name to give a C# type |
| `NodeStackType` | 54 | declared under `src/nodes/**`, the TSL / WebGPU node stack outside the extracted surface |
| `UntypedValue` | 45 | declared `any` / `unknown`, or with no type at all |
| `ConstructorOverloads` | 26 | the class declares more than one constructor |
| `UnwrappedClass` | 18 | an in-scope class that is itself not emitted |
| `AbstractClass` | 17 | the class is abstract, so it has no constructor to mirror |
| `ExternalType` | 14 | declared outside the scanned `src/` surface |
| `UnmappedTypeSyntax` | 12 | a TypeScript type form with no C# equivalent |
| `UnerasableTypeParameter` | 8 | a type parameter with neither a default nor a constraint to erase to |
| `RestParameter` | 6 | a rest parameter, including the rest-union-tuple pseudo-overload form |
| `NotExported` | 1 | the class is never exported, so the applier cannot reach it on `THREE` |
| `StringConstantGroup` | 1 | a group of string-valued constants, which a C# enum cannot carry over this wire format |

<details><summary>Every skipped member (1290)</summary>

| class | member | obstacle | why |
|---|---|---|---|
| `AmbientLight` | `property isAmbientLight` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `AnimationAction` | `method crossFadeFrom` | `UnmappedUnion` | parameter 'fadeOutAction': `AnimationAction` is not an emitted class: required parameter 'mixer' cannot be mapped: `AnimationMixer` is not an emitted class: required parameter 'root' cannot be mapped: `Object3D | AnimationObjectGroup` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `AnimationAction` | `method crossFadeTo` | `UnmappedUnion` | parameter 'fadeInAction': `AnimationAction` is not an emitted class: required parameter 'mixer' cannot be mapped: `AnimationMixer` is not an emitted class: required parameter 'root' cannot be mapped: `Object3D | AnimationObjectGroup` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `AnimationAction` | `method syncWith` | `UnmappedUnion` | parameter 'action': `AnimationAction` is not an emitted class: required parameter 'mixer' cannot be mapped: `AnimationMixer` is not an emitted class: required parameter 'root' cannot be mapped: `Object3D | AnimationObjectGroup` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `AnimationAction` | `method getMixer` | `UnmappedUnion` | return type: `AnimationMixer` is not an emitted class: required parameter 'root' cannot be mapped: `Object3D | AnimationObjectGroup` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `AnimationClip` | `property tracks` | `DomOrLibType` | `Array<KeyframeTrack>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `AnimationClip` | `property uuid` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `string`), and the wire format has no read op — C# could neither write it nor observe it |
| `AnimationClip` | `property userData` | `DomOrLibType` | `Record<string, unknown>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `AnimationClip` | `method parse` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `AnimationClip` | `method toJSON` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `AnimationClip` | `method CreateFromMorphTargetSequence` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `AnimationClip` | `method findByName` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `AnimationClip` | `method CreateClipsFromMorphTargetSequences` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `AnimationClip` | `method toJSON` | `OptionsInterface` | return type: `AnimationClipJSON` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `AnimationLoader` | `method parse` | `UnmappedTypeSyntax` | parameter 'json': `readonly unknown[]` is a TypeScript `typeOperator` type, which has no C# equivalent |
| `AnimationMixer` | `property _root` | `NotInstanceApi` | declared `protected`, so it is not part of the public API |
| `AnimationMixer` | `property _actions` | `NotInstanceApi` | declared `protected`, so it is not part of the public API |
| `AnimationMixer` | `property _nActiveActions` | `NotInstanceApi` | declared `protected`, so it is not part of the public API |
| `AnimationMixer` | `property _bindings` | `NotInstanceApi` | declared `protected`, so it is not part of the public API |
| `AnimationMixer` | `property _nActiveBindings` | `NotInstanceApi` | declared `protected`, so it is not part of the public API |
| `AnimationMixer` | `property _controlInterpolants` | `NotInstanceApi` | declared `protected`, so it is not part of the public API |
| `AnimationMixer` | `property _nActiveControlInterpolants` | `NotInstanceApi` | declared `protected`, so it is not part of the public API |
| `AnimationMixer` | `property _bindingsByRootAndName` | `NotInstanceApi` | declared `protected`, so it is not part of the public API |
| `AnimationMixer` | `property _actionsByClip` | `NotInstanceApi` | declared `protected`, so it is not part of the public API |
| `AnimationMixer` | `property _accuIndex` | `NotInstanceApi` | declared `protected`, so it is not part of the public API |
| `AnimationMixer` | `property stats` | `OptionsInterface` | `AnimationMixerStats` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `AnimationMixer` | `method clipAction` | `UnmappedUnion` | parameter 'optionalRoot': `Object3D | AnimationObjectGroup` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `AnimationMixer` | `method existingAction` | `UnmappedUnion` | parameter 'clip': `AnimationClip | string` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `AnimationMixer` | `method getRoot` | `UnmappedUnion` | return type: `Object3D | AnimationObjectGroup` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `AnimationMixer` | `method uncacheRoot` | `UnmappedUnion` | parameter 'root': `Object3D | AnimationObjectGroup` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `AnimationMixer` | `method uncacheAction` | `UnmappedUnion` | parameter 'clip': `AnimationClip | string` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `AnimationObjectGroup` | `property isAnimationObjectGroup` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `AnimationObjectGroup` | `property uuid` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `string`), and the wire format has no read op — C# could neither write it nor observe it |
| `AnimationObjectGroup` | `method add` | `RestParameter` | parameter 'args' is a rest parameter (`Object3D[]`) |
| `AnimationObjectGroup` | `method remove` | `RestParameter` | parameter 'args' is a rest parameter (`Object3D[]`) |
| `AnimationObjectGroup` | `method uncache` | `RestParameter` | parameter 'args' is a rest parameter (`Object3D[]`) |
| `AnimationUtils` | `method convertArray` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `AnimationUtils` | `method isTypedArray` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `AnimationUtils` | `method getKeyframeOrder` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `AnimationUtils` | `method sortedArray` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `AnimationUtils` | `method flattenJSON` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `AnimationUtils` | `method subclip` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `AnimationUtils` | `method makeClipAdditive` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `ArcCurve` | `property isArcCurve` | `UntypedValue` | the declaration carries no type |
| `ArcCurve` | `property type` | `UnmappedUnion` | `string | "ArcCurve"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `ArrayCamera` | `property isArrayCamera` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `ArrayCamera` | `property isMultiViewCamera` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `ArrayCamera` | `property cameras` | `CollectionType` | `PerspectiveCamera[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Audio` | `property listener` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `AudioListener`), and the wire format has no read op — C# could neither write it nor observe it |
| `Audio` | `property context` | `DomOrLibType` | `AudioContext` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Audio` | `property gain` | `DomOrLibType` | `GainNode` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Audio` | `property buffer` | `DomOrLibType` | `AudioBuffer` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Audio` | `property detune` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `Audio` | `property loop` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `Audio` | `property playbackRate` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `Audio` | `property isPlaying` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `Audio` | `property hasPlaybackControl` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `Audio` | `property source` | `DomOrLibType` | `AudioNode` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Audio` | `property sourceType` | `UnmappedUnion` | `"empty" | "audioNode" | "mediaNode" | "mediaStreamNode" | "buffer"` unions 5 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Audio` | `property filters` | `CollectionType` | `AudioNode[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Audio` | `method getOutput` | `DomOrLibType` | return type: `GainNode` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Audio` | `method setNodeSource` | `DomOrLibType` | parameter 'audioNode': `AudioNode` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Audio` | `method setMediaElementSource` | `DomOrLibType` | parameter 'mediaElement': `HTMLMediaElement` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Audio` | `method setMediaStreamSource` | `DomOrLibType` | parameter 'mediaStream': `MediaStream` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Audio` | `method setBuffer` | `DomOrLibType` | parameter 'audioBuffer': `AudioBuffer` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Audio` | `method play` | `UnmappedTypeSyntax` | return type: `this` is only meaningful as a fluent return type, which the caller handles separately |
| `Audio` | `method pause` | `UnmappedTypeSyntax` | return type: `this` is only meaningful as a fluent return type, which the caller handles separately |
| `Audio` | `method stop` | `UnmappedTypeSyntax` | return type: `this` is only meaningful as a fluent return type, which the caller handles separately |
| `Audio` | `method disconnect` | `UnmappedTypeSyntax` | return type: `this` is only meaningful as a fluent return type, which the caller handles separately |
| `Audio` | `method getFilters` | `CollectionType` | return type: `AudioNode[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Audio` | `method setFilters` | `CollectionType` | parameter 'value': `AudioNode[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Audio` | `method getFilter` | `DomOrLibType` | return type: `AudioNode` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Audio` | `method setFilter` | `DomOrLibType` | parameter 'filter': `AudioNode` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Audio` | `method setPlaybackRate` | `UnmappedTypeSyntax` | return type: `this` is only meaningful as a fluent return type, which the caller handles separately |
| `Audio` | `method setLoop` | `UnmappedTypeSyntax` | return type: `this` is only meaningful as a fluent return type, which the caller handles separately |
| `AudioAnalyser` | `property analyser` | `DomOrLibType` | `AnalyserNode` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `AudioAnalyser` | `property data` | `DomOrLibType` | `Uint8Array` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `AudioAnalyser` | `method getFrequencyData` | `DomOrLibType` | return type: `Uint8Array` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `AudioContext` | `method getContext` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `AudioContext` | `method setContext` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `AudioListener` | `property context` | `DomOrLibType` | `AudioContext` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `AudioListener` | `property gain` | `DomOrLibType` | `GainNode` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `AudioListener` | `property filter` | `DomOrLibType` | `AudioNode` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `AudioListener` | `property timeDelta` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `AudioListener` | `method getInput` | `DomOrLibType` | return type: `GainNode` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `AudioListener` | `method getFilter` | `DomOrLibType` | return type: `AudioNode` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `AudioListener` | `method setFilter` | `DomOrLibType` | parameter 'value': `AudioNode` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Backend` | `property renderer` | `AbstractClass` | `Renderer` is not an emitted class: required parameter 'backend' cannot be mapped: `Backend` is not an emitted class: the class is abstract, so it has no constructor to mirror |
| `Backend` | `property domElement` | `UnmappedUnion` | `HTMLCanvasElement | OffscreenCanvas | null` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Backend` | `property coordinateSystem` | `NotInstanceApi` | abstract, so there is no implementation to mirror |
| `Backend` | `method init` | `AbstractClass` | parameter 'renderer': `Renderer` is not an emitted class: required parameter 'backend' cannot be mapped: `Backend` is not an emitted class: the class is abstract, so it has no constructor to mirror |
| `Backend` | `method getDomElement` | `UnmappedUnion` | return type: `HTMLCanvasElement | OffscreenCanvas` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `BatchedMesh` | `property boundingBox` | `MathValueType` | `Box3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `BatchedMesh` | `property boundingSphere` | `MathValueType` | `Sphere` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `BatchedMesh` | `property customSort` | `CallbackType` | `(this: this, list: Array<{ start: number; count: number; z: number }>, camera: Camera) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `BatchedMesh` | `property maxInstanceCount` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `BatchedMesh` | `property instanceCount` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `BatchedMesh` | `property unusedVertexCount` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `BatchedMesh` | `property unusedIndexCount` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `BatchedMesh` | `property isBatchedMesh` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `BatchedMesh` | `method setCustomSort` | `CallbackType` | parameter 'sortFunction': `(this: this, list: Array<{ start: number; count: number; z: number }>, camera: Camera) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `BatchedMesh` | `method getGeometryRangeAt` | `OptionsInterface` | parameter 'target': `BatchedMeshGeometryRange` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `BatchedMesh` | `method setColorAt` | `UnmappedUnion` | parameter 'color': `Color | Vector4` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `BatchedMesh` | `method getBoundingBoxAt` | `MathValueType` | parameter 'target': `Box3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `BatchedMesh` | `method getBoundingSphereAt` | `MathValueType` | parameter 'target': `Sphere` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `BezierInterpolant` | `method interpolate_` | `UnmappedTypeAlias` | return type: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `BlendMode` | `property blendSrc` | `UnmappedTypeAlias` | `BlendingSrcFactor` aliases `BlendingDstFactor | typeof SrcAlphaSaturateFactor`, which is neither a group of numeric constants nor a type the mirror expresses |
| `BlendMode` | `property blendSrcAlpha` | `UnmappedTypeAlias` | `BlendingSrcFactor` aliases `BlendingDstFactor | typeof SrcAlphaSaturateFactor`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Bone` | `property isBone` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `Bone` | `property type` | `UnmappedUnion` | `string | "Bone"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Box2` | `property min` | `MathValueType` | `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box2` | `property max` | `MathValueType` | `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box2` | `method set` | `MathValueType` | parameter 'min': `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box2` | `method setFromPoints` | `CollectionType` | parameter 'points': `Vector2Like[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Box2` | `method setFromCenterAndSize` | `MathValueType` | parameter 'center': `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box2` | `method copy` | `MathValueType` | parameter 'box': `Box2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box2` | `method getCenter` | `MathValueType` | parameter 'target': `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box2` | `method getSize` | `MathValueType` | parameter 'target': `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box2` | `method expandByPoint` | `OptionsInterface` | parameter 'point': `Vector2Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Box2` | `method expandByVector` | `MathValueType` | parameter 'vector': `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box2` | `method containsPoint` | `MathValueType` | parameter 'point': `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box2` | `method containsBox` | `MathValueType` | parameter 'box': `Box2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box2` | `method getParameter` | `MathValueType` | parameter 'point': `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box2` | `method intersectsBox` | `MathValueType` | parameter 'box': `Box2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box2` | `method clampPoint` | `MathValueType` | parameter 'point': `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box2` | `method distanceToPoint` | `MathValueType` | parameter 'point': `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box2` | `method intersect` | `MathValueType` | parameter 'box': `Box2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box2` | `method union` | `MathValueType` | parameter 'box': `Box2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box2` | `method translate` | `MathValueType` | parameter 'offset': `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box2` | `method equals` | `MathValueType` | parameter 'box': `Box2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box2` | `method empty` | `UntypedValue` | return type: `any` carries no type information a C# signature could express |
| `Box2` | `method isIntersectionBox` | `UntypedValue` | parameter 'b': `any` carries no type information a C# signature could express |
| `Box3` | `property isBox3` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `Box3` | `method setFromArray` | `DomOrLibType` | parameter 'array': `ArrayLike<number>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Box3` | `method setFromBufferAttribute` | `UnmappedTypeAlias` | parameter 'attribute': `BufferAttribute` is not an emitted class: required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Box3` | `method setFromPoints` | `DomOrLibType` | parameter 'points': `Array<Vector3Like>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Box3` | `method copy` | `MathValueType` | parameter 'box': `Box3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box3` | `method expandByPoint` | `OptionsInterface` | parameter 'point': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Box3` | `method containsBox` | `MathValueType` | parameter 'box': `Box3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box3` | `method intersectsBox` | `MathValueType` | parameter 'box': `Box3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box3` | `method intersectsSphere` | `MathValueType` | parameter 'sphere': `Sphere` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box3` | `method intersectsPlane` | `MathValueType` | parameter 'plane': `Plane` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box3` | `method intersectsTriangle` | `MathValueType` | parameter 'triangle': `Triangle` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box3` | `method getBoundingSphere` | `MathValueType` | parameter 'target': `Sphere` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box3` | `method intersect` | `MathValueType` | parameter 'box': `Box3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box3` | `method union` | `MathValueType` | parameter 'box': `Box3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box3` | `method equals` | `MathValueType` | parameter 'box': `Box3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Box3` | `method toJSON` | `OptionsInterface` | return type: `Box3JSON` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Box3` | `method fromJSON` | `OptionsInterface` | parameter 'json': `Box3JSON` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Box3Helper` | `property box` | `MathValueType` | `Box3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `BoxGeometry` | `property type` | `UnmappedUnion` | `string | "BoxGeometry"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `BoxGeometry` | `property parameters` | `AnonymousObjectType` | `{ readonly width: number; readonly height: number; readonly depth: number; readonly widthSegments: number; readonly heightSegments: number; readonly depthSegments: number; }` is an anonymous object literal type with no named C# equivalent |
| `BoxGeometry` | `method fromJSON` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `BufferAttribute` | `property id` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `BufferAttribute` | `property array` | `UnmappedTypeAlias` | `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `BufferAttribute` | `property updateRanges` | `DomOrLibType` | `Array<{ /** * Position at which to start update. */ start: number; /** * The number of components to update. */ count: number; }>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `BufferAttribute` | `property count` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `BufferAttribute` | `property isBufferAttribute` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `BufferAttribute` | `property onUploadCallback` | `CallbackType` | `() => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `BufferAttribute` | `method onUpload` | `CallbackType` | parameter 'callback': `() => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `BufferAttribute` | `method copy` | `UnmappedTypeAlias` | parameter 'source': `BufferAttribute` is not an emitted class: required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `BufferAttribute` | `method copyAt` | `UnmappedTypeAlias` | parameter 'attribute': `BufferAttribute` is not an emitted class: required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `BufferAttribute` | `method copyArray` | `DomOrLibType` | parameter 'array': `ArrayLike<number>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `BufferAttribute` | `method applyMatrix3` | `MathValueType` | parameter 'm': `Matrix3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `BufferAttribute` | `method applyNormalMatrix` | `MathValueType` | parameter 'm': `Matrix3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `BufferAttribute` | `method set` | `UnmappedUnion` | parameter 'value': `ArrayLike<number> | ArrayBufferView` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `BufferAttribute` | `method toJSON` | `OptionsInterface` | return type: `BufferAttributeJSON` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `BufferGeometry` | `property type` | `UnmappedUnion` | `string | "BufferGeometry"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `BufferGeometry` | `property index` | `UnmappedTypeAlias` | `BufferAttribute` is not an emitted class: required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `BufferGeometry` | `property indirect` | `UnmappedTypeAlias` | `IndirectStorageBufferAttribute` is not an emitted class: required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `BufferGeometry` | `property indirectOffset` | `UnmappedUnion` | `number | number[]` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `BufferGeometry` | `property attributes` | `DomOrLibType` | `Record<string, BufferAttribute | InterleavedBufferAttribute>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `BufferGeometry` | `property morphAttributes` | `AnonymousObjectType` | `{ position?: Array<BufferAttribute | InterleavedBufferAttribute> | undefined; normal?: Array<BufferAttribute | InterleavedBufferAttribute> | undefined; color?: Array<BufferAttribute | InterleavedBufferAttribute> | undefined; }` is an anonymous object literal type with no named C# equivalent |
| `BufferGeometry` | `property groups` | `CollectionType` | `GeometryGroup[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `BufferGeometry` | `property boundingBox` | `MathValueType` | `Box3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `BufferGeometry` | `property boundingSphere` | `MathValueType` | `Sphere` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `BufferGeometry` | `property drawRange` | `AnonymousObjectType` | `{ start: number; count: number }` is an anonymous object literal type with no named C# equivalent |
| `BufferGeometry` | `property userData` | `DomOrLibType` | `Record<string, any>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `BufferGeometry` | `property isBufferGeometry` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `BufferGeometry` | `method getIndex` | `UnmappedTypeAlias` | return type: `BufferAttribute` is not an emitted class: required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `BufferGeometry` | `method setIndex` | `UnmappedUnion` | parameter 'index': `BufferAttribute | number[] | null` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `BufferGeometry` | `method setIndirect` | `UnmappedTypeAlias` | parameter 'indirect': `IndirectStorageBufferAttribute` is not an emitted class: required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `BufferGeometry` | `method getIndirect` | `UnmappedTypeAlias` | return type: `IndirectStorageBufferAttribute` is not an emitted class: required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `BufferGeometry` | `method setAttribute` | `UnerasableTypeParameter` | parameter 'name': type parameter `K` has neither a default nor a constraint, so erasing it leaves nothing to map to |
| `BufferGeometry` | `method getAttribute` | `UnerasableTypeParameter` | parameter 'name': type parameter `K` has neither a default nor a constraint, so erasing it leaves nothing to map to |
| `BufferGeometry` | `method deleteAttribute` | `UnmappedTypeSyntax` | parameter 'name': `keyof Attributes` is a TypeScript `typeOperator` type, which has no C# equivalent |
| `BufferGeometry` | `method hasAttribute` | `UnmappedTypeSyntax` | parameter 'name': `keyof Attributes` is a TypeScript `typeOperator` type, which has no C# equivalent |
| `BufferGeometry` | `method setFromPoints` | `UnmappedUnion` | parameter 'points': `Vector3[] | Vector2[]` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `BufferGeometry` | `method toJSON` | `OptionsInterface` | return type: `BufferGeometryJSON` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `BufferGeometryLoader` | `method parse` | `UntypedValue` | parameter 'json': `unknown` carries no type information a C# signature could express |
| `BundleGroup` | `property isBundleGroup` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `BundleGroup` | `property version` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `Camera` | `property isCamera` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `Camera` | `property viewport` | `MathValueType` | `Vector4` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Camera` | `property reversedDepth` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `CameraHelper` | `property pointMap` | `AnonymousObjectType` | `{ [x: string]: number[]; }` is an anonymous object literal type with no named C# equivalent |
| `CanvasTarget` | `property domElement` | `UnmappedUnion` | `HTMLCanvasElement | OffscreenCanvas` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `CanvasTarget` | `method getDrawingBufferSize` | `MathValueType` | parameter 'target': `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `CanvasTarget` | `method getSize` | `MathValueType` | parameter 'target': `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `CanvasTarget` | `method getScissor` | `MathValueType` | parameter 'target': `Vector4` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `CanvasTarget` | `method setScissor` | `MathValueType` | parameter 'x': `Vector4` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `CanvasTarget` | `method getViewport` | `MathValueType` | parameter 'target': `Vector4` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `CanvasTarget` | `method setViewport` | `MathValueType` | parameter 'x': `Vector4` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `CanvasTexture` | `property isCanvasTexture` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `CapsuleGeometry` | `property type` | `UnmappedUnion` | `string | "CapsuleGeometry"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `CapsuleGeometry` | `property parameters` | `AnonymousObjectType` | `{ readonly radius: number; readonly height: number; readonly capSegments: number; readonly radialSegments: number; readonly heightSegments: number; }` is an anonymous object literal type with no named C# equivalent |
| `CapsuleGeometry` | `method fromJSON` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `CatmullRomCurve3` | `property isCatmullRomCurve3` | `UntypedValue` | the declaration carries no type |
| `CatmullRomCurve3` | `property type` | `UnmappedUnion` | `string | "CatmullRomCurve3"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `CatmullRomCurve3` | `property points` | `CollectionType` | `Vector3[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `CatmullRomCurve3` | `property curveType` | `UnmappedTypeAlias` | `CurveType` aliases `"centripetal" | "chordal" | "catmullrom"`, which is neither a group of numeric constants nor a type the mirror expresses |
| `CircleGeometry` | `property type` | `UnmappedUnion` | `string | "CircleGeometry"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `CircleGeometry` | `property parameters` | `AnonymousObjectType` | `{ readonly radius: number; readonly segments: number; readonly thetaStart: number; readonly thetaLength: number; }` is an anonymous object literal type with no named C# equivalent |
| `CircleGeometry` | `method fromJSON` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `ClippingContext` | `property version` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `ClippingContext` | `property viewNormalMatrix` | `MathValueType` | `Matrix3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `ClippingContext` | `property clippingGroupContexts` | `DomOrLibType` | `WeakMap<ClippingGroup, ClippingContext>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `ClippingContext` | `property intersectionPlanes` | `CollectionType` | `Vector4[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `ClippingContext` | `property unionPlanes` | `CollectionType` | `Vector4[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `ClippingContext` | `property parentVersion` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number | null`), and the wire format has no read op — C# could neither write it nor observe it |
| `ClippingContext` | `property unionClippingCount` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `ClippingContext` | `method projectPlanes` | `CollectionType` | parameter 'source': `Plane[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `ClippingGroup` | `property isClippingGroup` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `ClippingGroup` | `property clippingPlanes` | `CollectionType` | `Plane[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Color` | `property isColor` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `Color` | `property NAMES` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Color` | `method set` | `RestParameter` | parameter 'args' is a rest-union-tuple pseudo-overload (`[color: ColorRepresentation] | [r: number, g: number, b: number]`), which is one TypeScript signature standing for several C# overloads |
| `Color` | `method getHSL` | `OptionsInterface` | parameter 'target': `HSL` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Color` | `method getRGB` | `OptionsInterface` | parameter 'target': `RGB` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Color` | `method applyMatrix3` | `MathValueType` | parameter 'm': `Matrix3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Color` | `method fromArray` | `UnmappedUnion` | parameter 'array': `number[] | ArrayLike<number>` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Color` | `method toArray` | `CollectionType` | parameter 'array': `number[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Color` | `method fromBufferAttribute` | `UnmappedUnion` | parameter 'attribute': `BufferAttribute | InterleavedBufferAttribute` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Color` | `method [Symbol.iterator]` | `NotInstanceApi` | the member name is not a usable C# identifier |
| `CompressedArrayTexture` | `property isCompressedArrayTexture` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `CompressedArrayTexture` | `property layerUpdates` | `DomOrLibType` | `Set<number>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `CompressedCubeTexture` | `property isCompressedCubeTexture` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `CompressedCubeTexture` | `property isCubeTexture` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `CompressedTexture` | `property isCompressedTexture` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `CompressedTexture` | `property mipmaps` | `CollectionType` | `CompressedTextureMipmap[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `CompressedTexture` | `property format` | `UnmappedTypeAlias` | `CompressedPixelFormat` cannot become a C# enum: constant `RGB_BPTC_SIGNED_Format` has no literal value in the IR (declared type `<none>`) |
| `CompressedTextureLoader` | `method load` | `CallbackType` | parameter 'onLoad': `(data: CompressedTexture) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `ConeGeometry` | `property type` | `UnmappedUnion` | `string | "ConeGeometry"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `ConeGeometry` | `property parameters` | `AnonymousObjectType` | `{ readonly radius: number; readonly radiusTop: number; readonly radiusBottom: number; readonly height: number; readonly radialSegments: number; readonly heightSegments: number; readonly openEnded: boolean; readonly thetaStart: number; readonly thetaLength: number; }` is an anonymous object literal type with no named C# equivalent |
| `ConeGeometry` | `method fromJSON` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Controls` | `property domElement` | `UnmappedUnion` | `HTMLElement | SVGElement | null` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Controls` | `method connect` | `UnmappedUnion` | parameter 'element': `HTMLElement | SVGElement` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `CubeCamera` | `method update` | `OptionsInterface` | parameter 'renderer': `CubeCameraRenderer` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `CubeDepthTexture` | `property isCubeDepthTexture` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `CubeDepthTexture` | `property isCubeTexture` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `CubeDepthTexture` | `property images` | `UnmappedTypeAlias` | `CubeDepthTextureImageData` aliases `[ DepthTextureImageData, DepthTextureImageData, DepthTextureImageData, DepthTextureImageData, DepthTextureImageData, DepthTextureImageData, ]`, which is neither a group of numeric constants nor a type the mirror expresses |
| `CubeRenderTarget` | `property isCubeRenderTarget` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `CubeRenderTarget` | `method fromEquirectangularTexture` | `AbstractClass` | parameter 'renderer': `Renderer` is not an emitted class: required parameter 'backend' cannot be mapped: `Backend` is not an emitted class: the class is abstract, so it has no constructor to mirror |
| `CubeRenderTarget` | `method clear` | `AbstractClass` | parameter 'renderer': `Renderer` is not an emitted class: required parameter 'backend' cannot be mapped: `Backend` is not an emitted class: the class is abstract, so it has no constructor to mirror |
| `CubeTexture` | `property isCubeTexture` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `CubeTexture` | `property images` | `CollectionType` | `TImage[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `CubeTextureLoader` | `method load` | `UnmappedTypeSyntax` | parameter 'url': `readonly string[]` is a TypeScript `typeOperator` type, which has no C# equivalent |
| `CubicBezierCurve` | `property isCubicBezierCurve` | `UntypedValue` | the declaration carries no type |
| `CubicBezierCurve` | `property type` | `UnmappedUnion` | `string | "CubicBezierCurve"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `CubicBezierCurve` | `property v0` | `MathValueType` | `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `CubicBezierCurve` | `property v1` | `MathValueType` | `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `CubicBezierCurve` | `property v2` | `MathValueType` | `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `CubicBezierCurve` | `property v3` | `MathValueType` | `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `CubicBezierCurve3` | `property isCubicBezierCurve3` | `UntypedValue` | the declaration carries no type |
| `CubicBezierCurve3` | `property type` | `UnmappedUnion` | `string | "CubicBezierCurve3"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `CubicInterpolant` | `method interpolate_` | `UnmappedTypeAlias` | return type: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Curve` | `property type` | `UnmappedUnion` | `string | "Curve"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Curve` | `method getPoint` | `UnmappedUnion` | parameter 'optionalTarget': `Vector2 | Vector3` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Curve` | `method getPointAt` | `UnmappedUnion` | parameter 'optionalTarget': `Vector2 | Vector3` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Curve` | `method getPoints` | `CollectionType` | return type: `TVector[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Curve` | `method getSpacedPoints` | `CollectionType` | return type: `TVector[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Curve` | `method getLengths` | `CollectionType` | return type: `number[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Curve` | `method getTangent` | `UnmappedUnion` | parameter 'optionalTarget': `Vector2 | Vector3` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Curve` | `method getTangentAt` | `UnmappedUnion` | parameter 'optionalTarget': `Vector2 | Vector3` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Curve` | `method computeFrenetFrames` | `AnonymousObjectType` | return type: `{ tangents: Vector3[]; normals: Vector3[]; binormals: Vector3[]; }` is an anonymous object literal type with no named C# equivalent |
| `Curve` | `method copy` | `AbstractClass` | parameter 'source': `Curve<TVector>` is not an emitted class: the class is abstract, so it has no constructor to mirror |
| `Curve` | `method toJSON` | `OptionsInterface` | return type: `CurveJSON` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Curve` | `method fromJSON` | `OptionsInterface` | parameter 'json': `CurveJSON` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `CurvePath` | `property type` | `UnmappedUnion` | `string | "CurvePath"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `CurvePath` | `property curves` | `DomOrLibType` | `Array<Curve<TVector>>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `CurvePath` | `method add` | `AbstractClass` | parameter 'curve': `Curve<TVector>` is not an emitted class: the class is abstract, so it has no constructor to mirror |
| `CurvePath` | `method getPoint` | `UnmappedUnion` | parameter 'optionalTarget': `Vector2 | Vector3` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `CurvePath` | `method getCurveLengths` | `CollectionType` | return type: `number[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `CurvePath` | `method getPoints` | `CollectionType` | return type: `TVector[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `CurvePath` | `method getSpacedPoints` | `CollectionType` | return type: `TVector[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `CurvePath` | `method toJSON` | `OptionsInterface` | return type: `CurvePathJSON` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `CurvePath` | `method fromJSON` | `OptionsInterface` | parameter 'json': `CurvePathJSON` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `CylinderGeometry` | `property type` | `UnmappedUnion` | `string | "CylinderGeometry"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `CylinderGeometry` | `property parameters` | `AnonymousObjectType` | `{ readonly radiusTop: number; readonly radiusBottom: number; readonly height: number; readonly radialSegments: number; readonly heightSegments: number; readonly openEnded: boolean; readonly thetaStart: number; readonly thetaLength: number; }` is an anonymous object literal type with no named C# equivalent |
| `CylinderGeometry` | `method fromJSON` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Cylindrical` | `method copy` | `MathValueType` | parameter 'other': `Cylindrical` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Data3DTexture` | `property isData3DTexture` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `DataArrayTexture` | `property isDataArrayTexture` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `DataArrayTexture` | `property layerUpdates` | `DomOrLibType` | `Set<number>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `DataTexture` | `property isDataTexture` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `DataTextureLoader` | `method load` | `CallbackType` | parameter 'onLoad': `(data: DataTexture, texData: DataTextureLoaderTexData) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `DataTextureLoader` | `method parse` | `NotInstanceApi` | abstract, so there is no implementation to mirror |
| `DataTextureLoader` | `method createDataTexture` | `DomOrLibType` | parameter 'buffer': `ArrayBuffer` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `DataUtils` | `method toHalfFloat` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `DataUtils` | `method fromHalfFloat` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `DepthTexture` | `property isDepthTexture` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `DirectionalLight` | `property isDirectionalLight` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `DirectionalLight` | `method toJSON` | `OptionsInterface` | parameter 'meta': `JSONMeta` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `DirectionalLightShadow` | `property isDirectionalLightShadow` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `DiscreteInterpolant` | `method interpolate_` | `UnmappedTypeAlias` | return type: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `DodecahedronGeometry` | `property type` | `UnmappedUnion` | `string | "DodecahedronGeometry"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `DodecahedronGeometry` | `method fromJSON` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Earcut` | `method triangulate` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `EdgesGeometry` | `property type` | `UnmappedUnion` | `string | "EdgesGeometry"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `EdgesGeometry` | `property parameters` | `AnonymousObjectType` | `{ readonly geometry: TBufferGeometry | null; readonly thresholdAngle: number; }` is an anonymous object literal type with no named C# equivalent |
| `EllipseCurve` | `property isEllipseCurve` | `UntypedValue` | the declaration carries no type |
| `EllipseCurve` | `property type` | `UnmappedUnion` | `string | "EllipseCurve"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Euler` | `property order` | `UnmappedTypeAlias` | `EulerOrder` aliases `"XYZ" | "YXZ" | "ZXY" | "ZYX" | "YZX" | "XZY"`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Euler` | `property isEuler` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `Euler` | `property _onChangeCallback` | `CallbackType` | `() => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `Euler` | `property DEFAULT_ORDER` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Euler` | `method set` | `UnmappedTypeAlias` | parameter 'order': `EulerOrder` aliases `"XYZ" | "YXZ" | "ZXY" | "ZYX" | "YZX" | "XZY"`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Euler` | `method setFromRotationMatrix` | `UnmappedTypeAlias` | parameter 'order': `EulerOrder` aliases `"XYZ" | "YXZ" | "ZXY" | "ZYX" | "YZX" | "XZY"`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Euler` | `method setFromQuaternion` | `UnmappedTypeAlias` | parameter 'order': `EulerOrder` aliases `"XYZ" | "YXZ" | "ZXY" | "ZYX" | "YZX" | "XZY"`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Euler` | `method setFromVector3` | `UnmappedTypeAlias` | parameter 'order': `EulerOrder` aliases `"XYZ" | "YXZ" | "ZXY" | "ZYX" | "YZX" | "XZY"`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Euler` | `method reorder` | `UnmappedTypeAlias` | parameter 'newOrder': `EulerOrder` aliases `"XYZ" | "YXZ" | "ZXY" | "ZYX" | "YZX" | "XZY"`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Euler` | `method fromArray` | `UnmappedTypeAlias` | parameter 'array': `EulerTuple` aliases `[x: number, y: number, z: number, order?: EulerOrder]`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Euler` | `method toArray` | `DomOrLibType` | parameter 'array': `Partial<EulerTuple>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Euler` | `method _onChange` | `CallbackType` | parameter 'callback': `() => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `Euler` | `method [Symbol.iterator]` | `NotInstanceApi` | the member name is not a usable C# identifier |
| `EventDispatcher` | `method addEventListener` | `UnerasableTypeParameter` | parameter 'type': type parameter `T` has neither a default nor a constraint, so erasing it leaves nothing to map to |
| `EventDispatcher` | `method hasEventListener` | `UnerasableTypeParameter` | parameter 'type': type parameter `T` has neither a default nor a constraint, so erasing it leaves nothing to map to |
| `EventDispatcher` | `method removeEventListener` | `UnerasableTypeParameter` | parameter 'type': type parameter `T` has neither a default nor a constraint, so erasing it leaves nothing to map to |
| `EventDispatcher` | `method dispatchEvent` | `UnmappedTypeSyntax` | parameter 'event': `BaseEvent<T> & TEventMap[T]` is a TypeScript `intersection` type, which has no C# equivalent |
| `ExternalTexture` | `property sourceTexture` | `UnmappedUnion` | `WebGLTexture | GPUTexture | null` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `ExternalTexture` | `property isExternalTexture` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `ExtrudeGeometry` | `property type` | `UnmappedUnion` | `string | "ExtrudeGeometry"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `ExtrudeGeometry` | `property parameters` | `AnonymousObjectType` | `{ readonly shapes: Shape | Shape[]; readonly options: ExtrudeGeometryOptions; }` is an anonymous object literal type with no named C# equivalent |
| `FileLoader` | `method load` | `CallbackType` | parameter 'onLoad': `(data: TData) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `Fog` | `property isFog` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `Fog` | `method toJSON` | `OptionsInterface` | return type: `FogJSON` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `FogExp2` | `property isFogExp2` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `FogExp2` | `method toJSON` | `OptionsInterface` | return type: `FogExp2JSON` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `FramebufferTexture` | `property isFramebufferTexture` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `Frustum` | `property planes` | `CollectionType` | `Plane[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Frustum` | `method set` | `MathValueType` | parameter 'p0': `Plane` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Frustum` | `method copy` | `MathValueType` | parameter 'frustum': `Frustum` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Frustum` | `method intersectsSphere` | `MathValueType` | parameter 'sphere': `Sphere` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Frustum` | `method intersectsBox` | `MathValueType` | parameter 'box': `Box3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `FrustumArray` | `method intersectsSphere` | `MathValueType` | parameter 'sphere': `Sphere` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `FrustumArray` | `method intersectsBox` | `MathValueType` | parameter 'box': `Box3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `FrustumArray` | `method copy` | `MathValueType` | parameter 'source': `FrustumArray` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `GLBufferAttribute` | `property isGLBufferAttribute` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `GLBufferAttribute` | `property buffer` | `DomOrLibType` | `WebGLBuffer` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `GLBufferAttribute` | `property type` | `DomOrLibType` | `GLenum` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `GLBufferAttribute` | `property elementSize` | `UnmappedUnion` | `1 | 2 | 4` unions 3 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `GLBufferAttribute` | `method setBuffer` | `DomOrLibType` | parameter 'buffer': `WebGLBuffer` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `GLBufferAttribute` | `method setType` | `DomOrLibType` | parameter 'type': `GLenum` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Group` | `property isGroup` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `HTMLTexture` | `property isHTMLTexture` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `HemisphereLight` | `property isHemisphereLight` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `HemisphereLight` | `method toJSON` | `OptionsInterface` | parameter 'meta': `JSONMeta` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `IESSpotLight` | `property iesMap` | `ConstructorOverloads` | `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `IcosahedronGeometry` | `property type` | `UnmappedUnion` | `string | "IcosahedronGeometry"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `IcosahedronGeometry` | `method fromJSON` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `ImageBitmapLoader` | `property isImageBitmapLoader` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `ImageBitmapLoader` | `property options` | `DomOrLibType` | `ImageBitmapOptions` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `ImageBitmapLoader` | `method setOptions` | `DomOrLibType` | parameter 'options': `ImageBitmapOptions` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `ImageBitmapLoader` | `method load` | `CallbackType` | parameter 'onLoad': `(data: ImageBitmap) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `ImageLoader` | `method load` | `CallbackType` | parameter 'onLoad': `(data: HTMLImageElement) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `ImageUtils` | `method getDataURL` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `ImageUtils` | `method sRGBToLinear` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `IndirectStorageBufferAttribute` | `property isIndirectStorageBufferAttribute` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `Info` | `property frame` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `Info` | `property calls` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `Info` | `property render` | `AnonymousObjectType` | `{ calls: number; frameCalls: number; drawCalls: number; triangles: number; points: number; lines: number; timestamp: number; }` is an anonymous object literal type with no named C# equivalent |
| `Info` | `property compute` | `AnonymousObjectType` | `{ calls: number; frameCalls: number; timestamp: number; }` is an anonymous object literal type with no named C# equivalent |
| `Info` | `property memory` | `AnonymousObjectType` | `{ attributes: number; attributesSize: number; geometries: number; indexAttributes: number; indexAttributesSize: number; indirectStorageAttributes: number; indirectStorageAttributesSize: number; programs: number; programsSize: number; readbackBuffers: number; readbackBuffersSize: number; renderTargets: number; storageAttributes: number; storageAttributesSize: number; textures: number; texturesSize: number; uniformBuffers: number; uniformBuffersSize: number; total: number; }` is an anonymous object literal type with no named C# equivalent |
| `Info` | `method createTexture` | `ConstructorOverloads` | parameter 'texture': `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `Info` | `method destroyTexture` | `ConstructorOverloads` | parameter 'texture': `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `Info` | `method createAttribute` | `UnmappedTypeAlias` | parameter 'attribute': `BufferAttribute` is not an emitted class: required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Info` | `method createIndexAttribute` | `UnmappedTypeAlias` | parameter 'attribute': `BufferAttribute` is not an emitted class: required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Info` | `method createStorageAttribute` | `UnmappedTypeAlias` | parameter 'attribute': `BufferAttribute` is not an emitted class: required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Info` | `method createIndirectStorageAttribute` | `UnmappedTypeAlias` | parameter 'attribute': `BufferAttribute` is not an emitted class: required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Info` | `method destroyAttribute` | `UnmappedTypeAlias` | parameter 'attribute': `BufferAttribute` is not an emitted class: required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `InspectorBase` | `property currentFrame` | `UntypedValue` | `unknown` carries no type information a C# signature could express |
| `InspectorBase` | `property nodeFrame` | `NodeStackType` | `NodeFrame` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `InspectorBase` | `method setRenderer` | `AbstractClass` | parameter 'renderer': `Renderer` is not an emitted class: required parameter 'backend' cannot be mapped: `Backend` is not an emitted class: the class is abstract, so it has no constructor to mirror |
| `InspectorBase` | `method getRenderer` | `AbstractClass` | return type: `Renderer` is not an emitted class: required parameter 'backend' cannot be mapped: `Backend` is not an emitted class: the class is abstract, so it has no constructor to mirror |
| `InspectorBase` | `method inspect` | `NodeStackType` | parameter 'node': `Node` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `InspectorBase` | `method computeAsync` | `NodeStackType` | parameter 'computeNode': `ComputeNode` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `InspectorBase` | `method beginCompute` | `NodeStackType` | parameter 'computeNode': `ComputeNode` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `InspectorBase` | `method copyTextureToTexture` | `ConstructorOverloads` | parameter 'srcTexture': `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `InspectorBase` | `method copyFramebufferToTexture` | `ConstructorOverloads` | parameter 'framebufferTexture': `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `InstancedBufferAttribute` | `property isInstancedBufferAttribute` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `InstancedBufferGeometry` | `property isInstancedBufferGeometry` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `InstancedMesh` | `property isInstancedMesh` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `InstancedMesh` | `property instanceMatrix` | `UnmappedTypeAlias` | `InstancedBufferAttribute` is not an emitted class: required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `InstancedMesh` | `property instanceColor` | `UnmappedTypeAlias` | `InstancedBufferAttribute` is not an emitted class: required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `InstancedMesh` | `property boundingBox` | `MathValueType` | `Box3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `InstancedMesh` | `property boundingSphere` | `MathValueType` | `Sphere` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `InstancedMesh` | `method toJSON` | `OptionsInterface` | parameter 'meta': `JSONMeta` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `InterleavedBuffer` | `property isInterleavedBuffer` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `InterleavedBuffer` | `property array` | `UnmappedTypeAlias` | `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `InterleavedBuffer` | `property updateRanges` | `DomOrLibType` | `Array<{ /** * Position at which to start update. */ start: number; /** * The number of components to update. */ count: number; }>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `InterleavedBuffer` | `property onUploadCallback` | `CallbackType` | `() => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `InterleavedBuffer` | `method onUpload` | `CallbackType` | parameter 'callback': `() => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `InterleavedBuffer` | `method set` | `DomOrLibType` | parameter 'value': `ArrayLike<number>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `InterleavedBuffer` | `method copy` | `UnmappedTypeAlias` | parameter 'source': `InterleavedBuffer` is not an emitted class: required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `InterleavedBuffer` | `method copyAt` | `UnmappedTypeAlias` | parameter 'attribute': `InterleavedBufferAttribute` is not an emitted class: required parameter 'interleavedBuffer' cannot be mapped: `InterleavedBuffer` is not an emitted class: required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `InterleavedBuffer` | `method clone` | `AnonymousObjectType` | parameter 'data': `{}` is an anonymous object literal type with no named C# equivalent |
| `InterleavedBuffer` | `method toJSON` | `AnonymousObjectType` | parameter 'data': `{}` is an anonymous object literal type with no named C# equivalent |
| `InterleavedBufferAttribute` | `property data` | `UnmappedTypeAlias` | `InterleavedBuffer` is not an emitted class: required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `InterleavedBufferAttribute` | `property count` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `InterleavedBufferAttribute` | `property array` | `UnmappedTypeAlias` | `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `InterleavedBufferAttribute` | `property isInterleavedBufferAttribute` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `InterleavedBufferAttribute` | `method applyNormalMatrix` | `MathValueType` | parameter 'm': `Matrix3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `InterleavedBufferAttribute` | `method clone` | `AnonymousObjectType` | parameter 'data': `{}` is an anonymous object literal type with no named C# equivalent |
| `InterleavedBufferAttribute` | `method toJSON` | `AnonymousObjectType` | parameter 'data': `{}` is an anonymous object literal type with no named C# equivalent |
| `Interpolant` | `property parameterPositions` | `UnmappedTypeAlias` | `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Interpolant` | `property resultBuffer` | `UnmappedTypeAlias` | `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Interpolant` | `property sampleValues` | `UnmappedTypeAlias` | `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Interpolant` | `property settings` | `AnonymousObjectType` | `{}` is an anonymous object literal type with no named C# equivalent |
| `Interpolant` | `property DefaultSettings_` | `AnonymousObjectType` | `{}` is an anonymous object literal type with no named C# equivalent |
| `Interpolant` | `method evaluate` | `UnmappedTypeAlias` | return type: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Interpolant` | `method getSettings_` | `UntypedValue` | return type: `unknown` carries no type information a C# signature could express |
| `Interpolant` | `method copySampleValue_` | `UnmappedTypeAlias` | return type: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Interpolant` | `method interpolate_` | `NotInstanceApi` | abstract, so there is no implementation to mirror |
| `KeyframeTrack` | `property times` | `DomOrLibType` | `Float32Array` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `KeyframeTrack` | `property values` | `DomOrLibType` | `Float32Array` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `KeyframeTrack` | `property TimeBufferType` | `UnmappedUnion` | `TypedArrayConstructor | ArrayConstructor` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `KeyframeTrack` | `property ValueBufferType` | `UnmappedUnion` | `TypedArrayConstructor | ArrayConstructor` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `KeyframeTrack` | `method toJSON` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `KeyframeTrack` | `method InterpolantFactoryMethodDiscrete` | `UnmappedTypeAlias` | parameter 'result': `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `KeyframeTrack` | `method InterpolantFactoryMethodLinear` | `UnmappedTypeAlias` | parameter 'result': `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `KeyframeTrack` | `method InterpolantFactoryMethodSmooth` | `UnmappedTypeAlias` | parameter 'result': `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `KeyframeTrack` | `method InterpolantFactoryMethodBezier` | `UnmappedTypeAlias` | parameter 'result': `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `LOD` | `property isLOD` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `LOD` | `property type` | `UnmappedUnion` | `string | "LOD"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `LOD` | `property levels` | `DomOrLibType` | `Array<{ /** The Object3D to display at this level. */ object: Object3D; /** The distance at which to display this level of detail. Expects a `Float`. */ distance: number; /** Threshold used to avoid flickering at LOD boundaries, as a fraction of distance. Expects a `Float`. */ hysteresis: number; }>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `LOD` | `method toJSON` | `OptionsInterface` | parameter 'meta': `JSONMeta` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `LatheGeometry` | `property type` | `UnmappedUnion` | `string | "LatheGeometry"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `LatheGeometry` | `property parameters` | `AnonymousObjectType` | `{ readonly points: Vector2[]; readonly segments: number; readonly phiStart: number; readonly phiLength: number; }` is an anonymous object literal type with no named C# equivalent |
| `LatheGeometry` | `method fromJSON` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Light` | `property isLight` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `Light` | `method copy` | `AbstractClass` | parameter 'source': `Light` is not an emitted class: the class is abstract, so it has no constructor to mirror |
| `Light` | `method toJSON` | `OptionsInterface` | parameter 'meta': `JSONMeta` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `LightProbe` | `property isLightProbe` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `LightProbe` | `property sh` | `MathValueType` | `SphericalHarmonics3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `LightProbe` | `method toJSON` | `OptionsInterface` | parameter 'meta': `JSONMeta` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `LightShadow` | `property biasNode` | `NodeStackType` | `Node<"float">` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `LightShadow` | `property mapSize` | `MathValueType` | `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `LightShadow` | `method getFrustum` | `MathValueType` | return type: `Frustum` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `LightShadow` | `method updateMatrices` | `AbstractClass` | parameter 'light': `Light` is not an emitted class: the class is abstract, so it has no constructor to mirror |
| `LightShadow` | `method getViewport` | `MathValueType` | return type: `Vector4` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `LightShadow` | `method getFrameExtents` | `MathValueType` | return type: `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `LightShadow` | `method copy` | `AbstractClass` | parameter 'source': `LightShadow` is not an emitted class: the class is abstract, so it has no constructor to mirror |
| `LightShadow` | `method toJSON` | `OptionsInterface` | return type: `LightShadowJSON` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Lighting` | `method createNode` | `DomOrLibType` | parameter 'lights': `Array<Light>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Lighting` | `method getNode` | `NodeStackType` | return type: `LightsNode` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `Line` | `property isLine` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `Line` | `property type` | `UnmappedUnion` | `string | "Line"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Line` | `property material` | `UnmappedUnion` | `Material | Material[]` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Line` | `property morphTargetInfluences` | `CollectionType` | `number[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Line` | `property morphTargetDictionary` | `AnonymousObjectType` | `{ [key: string]: number }` is an anonymous object literal type with no named C# equivalent |
| `Line2NodeMaterial` | `property isLine2NodeMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `Line2NodeMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `Line2NodeMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `Line3` | `method copy` | `MathValueType` | parameter 'line': `Line3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Line3` | `method distanceSqToLine3` | `MathValueType` | parameter 'line': `Line3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Line3` | `method equals` | `MathValueType` | parameter 'line': `Line3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `LineBasicMaterial` | `property isLineBasicMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `LineBasicMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `LineBasicMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `LineBasicNodeMaterial` | `property isLineBasicNodeMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `LineBasicNodeMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `LineBasicNodeMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `LineCurve` | `property isLineCurve` | `UntypedValue` | the declaration carries no type |
| `LineCurve` | `property type` | `UnmappedUnion` | `string | "LineCurve"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `LineCurve` | `property v1` | `MathValueType` | `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `LineCurve` | `property v2` | `MathValueType` | `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `LineCurve3` | `property isLineCurve3` | `UntypedValue` | the declaration carries no type |
| `LineCurve3` | `property type` | `UnmappedUnion` | `string | "LineCurve3"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `LineDashedMaterial` | `property isLineDashedMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `LineDashedMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `LineDashedMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `LineDashedNodeMaterial` | `property isLineDashedNodeMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `LineDashedNodeMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `LineDashedNodeMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `LineLoop` | `property isLineLoop` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `LineLoop` | `property type` | `UnmappedUnion` | `string | "LineLoop"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `LineSegments` | `property isLineSegments` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `LineSegments` | `property type` | `UnmappedUnion` | `string | "LineSegments"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `LinearInterpolant` | `method interpolate_` | `UnmappedTypeAlias` | return type: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Loader` | `property requestHeader` | `AnonymousObjectType` | `{ [header: string]: string }` is an anonymous object literal type with no named C# equivalent |
| `Loader` | `property DEFAULT_MATERIAL_NAME` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Loader` | `method load` | `CallbackType` | parameter 'onLoad': `(data: TData) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `Loader` | `method loadAsync` | `CallbackType` | parameter 'onProgress': `(event: ProgressEvent) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `Loader` | `method setRequestHeader` | `AnonymousObjectType` | parameter 'requestHeader': `{ [header: string]: string }` is an anonymous object literal type with no named C# equivalent |
| `LoaderUtils` | `method extractUrlBase` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `LoaderUtils` | `method resolveURL` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `LoadingManager` | `property onStart` | `CallbackType` | `(url: string, loaded: number, total: number) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `LoadingManager` | `property onLoad` | `CallbackType` | `() => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `LoadingManager` | `property onProgress` | `CallbackType` | `(url: string, loaded: number, total: number) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `LoadingManager` | `property onError` | `CallbackType` | `(url: string) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `LoadingManager` | `property abortController` | `DomOrLibType` | `AbortController` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `LoadingManager` | `property itemStart` | `CallbackType` | `(url: string) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `LoadingManager` | `property itemEnd` | `CallbackType` | `(url: string) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `LoadingManager` | `property itemError` | `CallbackType` | `(url: string) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `LoadingManager` | `property resolveURL` | `CallbackType` | `(url: string) => string` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `LoadingManager` | `property setURLModifier` | `CallbackType` | `(callback?: (url: string) => string) => this` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `LoadingManager` | `property addHandler` | `CallbackType` | `(regex: RegExp, loader: Loader) => this` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `LoadingManager` | `property removeHandler` | `CallbackType` | `(regex: RegExp) => this` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `LoadingManager` | `property getHandler` | `CallbackType` | `(file: string) => Loader | null` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `LoadingManager` | `property abort` | `CallbackType` | `() => this` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `Material` | `property isMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `Material` | `property uuid` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `string`), and the wire format has no read op — C# could neither write it nor observe it |
| `Material` | `property version` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `Material` | `property defines` | `DomOrLibType` | `Record<string, unknown>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Material` | `method onBeforeCompile` | `OptionsInterface` | parameter 'parameters': `WebGLProgramParametersWithUniforms` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Material` | `method setValues` | `OptionsInterface` | parameter 'values': `MaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `Material` | `method toJSON` | `OptionsInterface` | parameter 'meta': `JSONMeta` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Material` | `method fromJSON` | `OptionsInterface` | parameter 'json': `MaterialJSON` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `MaterialLoader` | `property textures` | `AnonymousObjectType` | `{ [key: string]: Texture }` is an anonymous object literal type with no named C# equivalent |
| `MaterialLoader` | `method parse` | `UntypedValue` | parameter 'json': `unknown` carries no type information a C# signature could express |
| `MaterialLoader` | `method setTextures` | `AnonymousObjectType` | parameter 'textures': `{ [key: string]: Texture }` is an anonymous object literal type with no named C# equivalent |
| `MaterialLoader` | `method createMaterialFromType` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `MaterialLoader` | `method registerMaterial` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Matrix2` | `property isMatrix2` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `Matrix2` | `property elements` | `UnmappedTypeAlias` | `Matrix2Tuple` aliases `[ n11: number, n12: number, n21: number, n22: number, ]`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Matrix2` | `method fromArray` | `DomOrLibType` | parameter 'array': `ArrayLike<number>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Matrix3` | `property isMatrix3` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `Matrix3` | `property elements` | `UnmappedTypeAlias` | `Matrix3Tuple` aliases `[ n11: number, n12: number, n13: number, n21: number, n22: number, n23: number, n31: number, n32: number, n33: number, ]`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Matrix3` | `method copy` | `MathValueType` | parameter 'm': `Matrix3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Matrix3` | `method multiply` | `MathValueType` | parameter 'm': `Matrix3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Matrix3` | `method premultiply` | `MathValueType` | parameter 'm': `Matrix3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Matrix3` | `method multiplyMatrices` | `MathValueType` | parameter 'a': `Matrix3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Matrix3` | `method transposeIntoArray` | `CollectionType` | parameter 'r': `number[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Matrix3` | `method makeTranslation` | `MathValueType` | parameter 'v': `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Matrix3` | `method equals` | `MathValueType` | parameter 'matrix': `Matrix3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Matrix3` | `method fromArray` | `DomOrLibType` | parameter 'array': `ArrayLike<number>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Matrix3` | `method toArray` | `UnmappedTypeAlias` | return type: `Matrix3Tuple` aliases `[ n11: number, n12: number, n13: number, n21: number, n22: number, n23: number, n31: number, n32: number, n33: number, ]`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Matrix4` | `property elements` | `UnmappedTypeAlias` | `Matrix4Tuple` aliases `[ n11: number, n12: number, n13: number, n14: number, n21: number, n22: number, n23: number, n24: number, n31: number, n32: number, n33: number, n34: number, n41: number, n42: number, n43: number, n44: number, ]`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Matrix4` | `method setFromMatrix3` | `MathValueType` | parameter 'm': `Matrix3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Matrix4` | `method fromArray` | `DomOrLibType` | parameter 'array': `ArrayLike<number>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Matrix4` | `method toArray` | `UnerasableTypeParameter` | parameter 'array': type parameter `TArray` has neither a default nor a constraint, so erasing it leaves nothing to map to |
| `Mesh` | `property isMesh` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `Mesh` | `property type` | `UnmappedUnion` | `string | "Mesh"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Mesh` | `property material` | `UnmappedUnion` | `Material | Material[]` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Mesh` | `property morphTargetInfluences` | `CollectionType` | `number[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Mesh` | `property morphTargetDictionary` | `AnonymousObjectType` | `{ [key: string]: number }` is an anonymous object literal type with no named C# equivalent |
| `Mesh` | `method toJSON` | `OptionsInterface` | parameter 'meta': `JSONMeta` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `MeshBasicMaterial` | `property isMeshBasicMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshBasicMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `MeshBasicMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `MeshBasicNodeMaterial` | `property isMeshBasicNodeMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshBasicNodeMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `MeshBasicNodeMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `MeshBasicNodeMaterial` | `method setupEnvironment` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `MeshBasicNodeMaterial` | `method setupLightingModel` | `NodeStackType` | return type: `BasicLightingModel` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `MeshDepthMaterial` | `property isMeshDepthMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshDepthMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `MeshDepthMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `MeshDistanceMaterial` | `property isMeshDistanceMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshDistanceMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `MeshDistanceMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `MeshLambertMaterial` | `property isMeshLambertMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshLambertMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `MeshLambertMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `MeshLambertNodeMaterial` | `property isMeshLambertNodeMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshLambertNodeMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `MeshLambertNodeMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `MeshLambertNodeMaterial` | `method setupEnvironment` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `MeshLambertNodeMaterial` | `method setupLightingModel` | `NodeStackType` | return type: `PhongLightingModel` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `MeshMatcapMaterial` | `property isMeshMatcapMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshMatcapMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `MeshMatcapMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `MeshMatcapNodeMaterial` | `property isMeshMatcapNodeMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshMatcapNodeMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `MeshMatcapNodeMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `MeshMatcapNodeMaterial` | `method setupVariants` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `MeshNormalMaterial` | `property isMeshNormalMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshNormalMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `MeshNormalMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `MeshNormalNodeMaterial` | `property isMeshNormalNodeMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshNormalNodeMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `MeshNormalNodeMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `MeshPhongMaterial` | `property isMeshPhongMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshPhongMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `MeshPhongMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `MeshPhongNodeMaterial` | `property isMeshPhongNodeMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshPhongNodeMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `MeshPhongNodeMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `MeshPhongNodeMaterial` | `method setupEnvironment` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `MeshPhongNodeMaterial` | `method setupLightingModel` | `NodeStackType` | return type: `PhongLightingModel` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `MeshPhysicalMaterial` | `property isMeshPhysicalMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshPhysicalMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `MeshPhysicalMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `MeshPhysicalNodeMaterial` | `property isMeshPhysicalNodeMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshPhysicalNodeMaterial` | `property useClearcoat` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshPhysicalNodeMaterial` | `property useIridescence` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshPhysicalNodeMaterial` | `property useSheen` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshPhysicalNodeMaterial` | `property useAnisotropy` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshPhysicalNodeMaterial` | `property useTransmission` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshPhysicalNodeMaterial` | `property useDispersion` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshPhysicalNodeMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `MeshPhysicalNodeMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `MeshPhysicalNodeMaterial` | `method setupVariants` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `MeshPhysicalNodeMaterial` | `method setupClearcoatNormal` | `NodeStackType` | return type: `Node` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `MeshSSSNodeMaterial` | `property useSSS` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshSSSNodeMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `MeshSSSNodeMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `MeshSSSNodeMaterial` | `method setupLightingModel` | `NotExported` | return type: `SSSLightingModel` is not an emitted class: the class is never exported, so it is not reachable on the `THREE` namespace the applier looks names up on |
| `MeshStandardMaterial` | `property isMeshStandardMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshStandardMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `MeshStandardMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `MeshStandardNodeMaterial` | `property isMeshStandardNodeMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshStandardNodeMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `MeshStandardNodeMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `MeshStandardNodeMaterial` | `method setupEnvironment` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `MeshStandardNodeMaterial` | `method setupLightingModel` | `NodeStackType` | return type: `PhysicalLightingModel` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `MeshToonMaterial` | `property isMeshToonMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshToonMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `MeshToonMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `MeshToonNodeMaterial` | `property isMeshToonNodeMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `MeshToonNodeMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `MeshToonNodeMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `MeshToonNodeMaterial` | `method setupLightingModel` | `NodeStackType` | return type: `ToonLightingModel` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeLoader` | `property textures` | `AnonymousObjectType` | `{ [key: string]: Texture }` is an anonymous object literal type with no named C# equivalent |
| `NodeLoader` | `property nodes` | `AnonymousObjectType` | `{ [type: string]: Node }` is an anonymous object literal type with no named C# equivalent |
| `NodeLoader` | `method parseNodes` | `UntypedValue` | parameter 'json': `unknown` carries no type information a C# signature could express |
| `NodeLoader` | `method parse` | `UntypedValue` | parameter 'json': `unknown` carries no type information a C# signature could express |
| `NodeLoader` | `method setTextures` | `AnonymousObjectType` | parameter 'textures': `{ [key: string]: Texture }` is an anonymous object literal type with no named C# equivalent |
| `NodeLoader` | `method setNodes` | `AnonymousObjectType` | parameter 'value': `{ [type: string]: Node }` is an anonymous object literal type with no named C# equivalent |
| `NodeLoader` | `method createNodeFromType` | `NodeStackType` | return type: `Node` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterial` | `property type` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `NodeMaterial` | `property isNodeMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `NodeMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `NodeMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `NodeMaterial` | `method build` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterial` | `method setupObserver` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterial` | `method setup` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterial` | `method setupClipping` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterial` | `method setupHardwareClipping` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterial` | `method setupDepth` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterial` | `method setupPositionView` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterial` | `method setupModelViewProjection` | `NodeStackType` | return type: `Node` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterial` | `method setupVertex` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterial` | `method setupPosition` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterial` | `method setupDiffuseColor` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterial` | `method setupVariants` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterial` | `method setupOutgoingLight` | `NodeStackType` | return type: `Node` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterial` | `method setupNormal` | `NodeStackType` | return type: `Node` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterial` | `method setupEnvironment` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterial` | `method setupLightMap` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterial` | `method setupMaterialLightings` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterial` | `method setupAmbientOcclusion` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterial` | `method setupLightingModel` | `NodeStackType` | return type: `LightingModel` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterial` | `method setupLighting` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterial` | `method setupFog` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterial` | `method setupPremultipliedAlpha` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterial` | `method setupOutput` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeMaterialLoader` | `property nodes` | `OptionsInterface` | `NodeLoaderResult` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `NodeMaterialLoader` | `property nodeMaterials` | `AnonymousObjectType` | `{ [type: string]: NodeMaterial }` is an anonymous object literal type with no named C# equivalent |
| `NodeMaterialLoader` | `method setNodes` | `OptionsInterface` | parameter 'value': `NodeLoaderResult` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `NodeMaterialLoader` | `method setNodeMaterials` | `AnonymousObjectType` | parameter 'value': `{ [type: string]: NodeMaterial }` is an anonymous object literal type with no named C# equivalent |
| `NodeMaterialObserver` | `property refreshUniforms` | `CollectionType` | `string[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `NodeMaterialObserver` | `method needsVelocity` | `AbstractClass` | parameter 'renderer': `Renderer` is not an emitted class: required parameter 'backend' cannot be mapped: `Backend` is not an emitted class: the class is abstract, so it has no constructor to mirror |
| `NodeMaterialObserver` | `method containsNode` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `NodeObjectLoader` | `property nodes` | `AnonymousObjectType` | `{ [type: string]: Node }` is an anonymous object literal type with no named C# equivalent |
| `NodeObjectLoader` | `property nodeMaterials` | `AnonymousObjectType` | `{ [type: string]: NodeMaterial }` is an anonymous object literal type with no named C# equivalent |
| `NodeObjectLoader` | `method setNodes` | `AnonymousObjectType` | parameter 'value': `{ [type: string]: Node }` is an anonymous object literal type with no named C# equivalent |
| `NodeObjectLoader` | `method setNodeMaterials` | `AnonymousObjectType` | parameter 'value': `{ [type: string]: NodeMaterial }` is an anonymous object literal type with no named C# equivalent |
| `NodeObjectLoader` | `method parseNodes` | `UntypedValue` | parameter 'json': `unknown` carries no type information a C# signature could express |
| `NodeObjectLoader` | `method parseMaterials` | `UntypedValue` | parameter 'json': `unknown` carries no type information a C# signature could express |
| `Object3D` | `property isObject3D` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `Object3D` | `property id` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `Object3D` | `property type` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `string`), and the wire format has no read op — C# could neither write it nor observe it |
| `Object3D` | `property children` | `CollectionType` | `Object3D[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Object3D` | `property normalMatrix` | `MathValueType` | `Matrix3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Object3D` | `property animations` | `CollectionType` | `AnimationClip[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Object3D` | `property userData` | `DomOrLibType` | `Record<string, any>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Object3D` | `property DEFAULT_UP` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Object3D` | `property DEFAULT_MATRIX_AUTO_UPDATE` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Object3D` | `property DEFAULT_MATRIX_WORLD_AUTO_UPDATE` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Object3D` | `method add` | `RestParameter` | parameter 'object' is a rest parameter (`Object3D[]`) |
| `Object3D` | `method remove` | `RestParameter` | parameter 'object' is a rest parameter (`Object3D[]`) |
| `Object3D` | `method getObjectByProperty` | `UntypedValue` | parameter 'value': `any` carries no type information a C# signature could express |
| `Object3D` | `method getObjectsByProperty` | `UntypedValue` | parameter 'value': `any` carries no type information a C# signature could express |
| `Object3D` | `method raycast` | `CollectionType` | parameter 'intersects': `Intersection[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Object3D` | `method traverse` | `CallbackType` | parameter 'callback': `(object: Object3D) => any` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `Object3D` | `method traverseVisible` | `CallbackType` | parameter 'callback': `(object: Object3D) => any` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `Object3D` | `method traverseAncestors` | `CallbackType` | parameter 'callback': `(object: Object3D) => any` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `Object3D` | `method toJSON` | `OptionsInterface` | parameter 'meta': `JSONMeta` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `ObjectLoader` | `method load` | `CallbackType` | parameter 'onLoad': `(data: Object3D) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `ObjectLoader` | `method parse` | `UntypedValue` | parameter 'json': `unknown` carries no type information a C# signature could express |
| `ObjectLoader` | `method parseAsync` | `UntypedValue` | parameter 'json': `unknown` carries no type information a C# signature could express |
| `ObjectLoader` | `method parseGeometries` | `UntypedValue` | parameter 'json': `unknown` carries no type information a C# signature could express |
| `ObjectLoader` | `method parseMaterials` | `UntypedValue` | parameter 'json': `unknown` carries no type information a C# signature could express |
| `ObjectLoader` | `method parseAnimations` | `UntypedValue` | parameter 'json': `unknown` carries no type information a C# signature could express |
| `ObjectLoader` | `method parseImages` | `UntypedValue` | parameter 'json': `unknown` carries no type information a C# signature could express |
| `ObjectLoader` | `method parseImagesAsync` | `UntypedValue` | parameter 'json': `unknown` carries no type information a C# signature could express |
| `ObjectLoader` | `method parseTextures` | `UntypedValue` | parameter 'json': `unknown` carries no type information a C# signature could express |
| `ObjectLoader` | `method parseObject` | `UntypedValue` | parameter 'data': `unknown` carries no type information a C# signature could express |
| `OctahedronGeometry` | `property type` | `UnmappedUnion` | `string | "OctahedronGeometry"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `OctahedronGeometry` | `method fromJSON` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `OrthographicCamera` | `property isOrthographicCamera` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `OrthographicCamera` | `property view` | `AnonymousObjectType` | `{ enabled: boolean; fullWidth: number; fullHeight: number; offsetX: number; offsetY: number; width: number; height: number; }` is an anonymous object literal type with no named C# equivalent |
| `OrthographicCamera` | `method toJSON` | `OptionsInterface` | parameter 'meta': `JSONMeta` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `PMREMGenerator` | `method fromScene` | `OptionsInterface` | parameter 'options': `PMREMGeneratorOptions` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `PMREMGenerator` | `method fromEquirectangular` | `ConstructorOverloads` | parameter 'equirectangular': `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `PMREMGenerator` | `method fromScene` | `OptionsInterface` | parameter 'options': `PMREMGeneratorOptions` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `PMREMGenerator` | `method fromSceneAsync` | `OptionsInterface` | parameter 'options': `PMREMGeneratorOptions` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `PMREMGenerator` | `method fromEquirectangular` | `ConstructorOverloads` | parameter 'equirectangular': `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `PMREMGenerator` | `method fromEquirectangularAsync` | `ConstructorOverloads` | parameter 'equirectangular': `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `PMREMGenerator` | `method fromCubemap` | `ConstructorOverloads` | parameter 'cubemap': `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `PMREMGenerator` | `method fromCubemapAsync` | `ConstructorOverloads` | parameter 'cubemap': `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `PMREMGenerator` | `method compileCubemapShader` | `DomOrLibType` | return type: `Promise<void>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `PMREMGenerator` | `method compileEquirectangularShader` | `DomOrLibType` | return type: `Promise<void>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Path` | `property type` | `UnmappedUnion` | `string | "Path"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Path` | `property currentPoint` | `MathValueType` | `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Path` | `method setFromPoints` | `CollectionType` | parameter 'vectors': `Vector2[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Path` | `method splineThru` | `CollectionType` | parameter 'pts': `Vector2[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Path` | `method toJSON` | `OptionsInterface` | return type: `PathJSON` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Path` | `method fromJSON` | `OptionsInterface` | parameter 'json': `PathJSON` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `PerspectiveCamera` | `property isPerspectiveCamera` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `PerspectiveCamera` | `property view` | `AnonymousObjectType` | `{ enabled: boolean; fullWidth: number; fullHeight: number; offsetX: number; offsetY: number; width: number; height: number; }` is an anonymous object literal type with no named C# equivalent |
| `PerspectiveCamera` | `method getViewBounds` | `MathValueType` | parameter 'minTarget': `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `PerspectiveCamera` | `method getViewSize` | `MathValueType` | parameter 'target': `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `PerspectiveCamera` | `method toJSON` | `OptionsInterface` | parameter 'meta': `JSONMeta` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Plane` | `property isPlane` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `Plane` | `method copy` | `MathValueType` | parameter 'plane': `Plane` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Plane` | `method distanceToSphere` | `MathValueType` | parameter 'sphere': `Sphere` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Plane` | `method intersectLine` | `MathValueType` | parameter 'line': `Line3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Plane` | `method intersectsLine` | `MathValueType` | parameter 'line': `Line3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Plane` | `method intersectsBox` | `MathValueType` | parameter 'box': `Box3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Plane` | `method intersectsSphere` | `MathValueType` | parameter 'sphere': `Sphere` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Plane` | `method applyMatrix4` | `MathValueType` | parameter 'optionalNormalMatrix': `Matrix3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Plane` | `method equals` | `MathValueType` | parameter 'plane': `Plane` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Plane` | `method isIntersectionLine` | `UntypedValue` | parameter 'l': `any` carries no type information a C# signature could express |
| `PlaneGeometry` | `property type` | `UnmappedUnion` | `string | "PlaneGeometry"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `PlaneGeometry` | `property parameters` | `AnonymousObjectType` | `{ readonly width: number; readonly height: number; readonly widthSegments: number; readonly heightSegments: number; }` is an anonymous object literal type with no named C# equivalent |
| `PlaneGeometry` | `method fromJSON` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `PlaneHelper` | `property plane` | `MathValueType` | `Plane` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `PointLight` | `property isPointLight` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `PointLight` | `method toJSON` | `OptionsInterface` | parameter 'meta': `JSONMeta` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `PointLightShadow` | `property isPointLightShadow` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `Points` | `property isPoints` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `Points` | `property type` | `UnmappedUnion` | `string | "Points"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Points` | `property morphTargetInfluences` | `CollectionType` | `number[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Points` | `property morphTargetDictionary` | `AnonymousObjectType` | `{ [key: string]: number }` is an anonymous object literal type with no named C# equivalent |
| `Points` | `property material` | `UnmappedUnion` | `Material | Material[]` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `PointsMaterial` | `property isPointsMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `PointsMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `PointsMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `PointsNodeMaterial` | `property isPointsNodeMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `PointsNodeMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `PointsNodeMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `PolyhedronGeometry` | `property type` | `UnmappedUnion` | `string | "PolyhedronGeometry"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `PolyhedronGeometry` | `property parameters` | `AnonymousObjectType` | `{ readonly vertices: number[]; readonly indices: number[]; readonly radius: number; readonly detail: number; }` is an anonymous object literal type with no named C# equivalent |
| `PolyhedronGeometry` | `method fromJSON` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `PositionalAudio` | `property panner` | `DomOrLibType` | `PannerNode` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `PositionalAudio` | `method getDistanceModel` | `UnmappedUnion` | return type: `"linear" | "inverse" | "exponential"` unions 3 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `PositionalAudio` | `method setDistanceModel` | `UnmappedUnion` | parameter 'value': `"linear" | "inverse" | "exponential"` unions 3 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `PropertyBinding` | `property parsedPath` | `UntypedValue` | `object` carries no type information a C# signature could express |
| `PropertyBinding` | `property node` | `UntypedValue` | `object` carries no type information a C# signature could express |
| `PropertyBinding` | `property rootNode` | `UnmappedUnion` | `Object3D | Skeleton` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `PropertyBinding` | `method create` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `PropertyBinding` | `method sanitizeNodeName` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `PropertyBinding` | `method parseTrackName` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `PropertyBinding` | `method findNode` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `PropertyMixer` | `property binding` | `UnmappedUnion` | `PropertyBinding` is not an emitted class: required parameter 'rootNode' cannot be mapped: `Object3D | Skeleton` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `PropertyMixer` | `property buffer` | `UnmappedUnion` | `Float64Array | unknown[]` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `QuadMesh` | `property isQuadMesh` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `QuadMesh` | `method renderAsync` | `AbstractClass` | parameter 'renderer': `Renderer` is not an emitted class: required parameter 'backend' cannot be mapped: `Backend` is not an emitted class: the class is abstract, so it has no constructor to mirror |
| `QuadMesh` | `method render` | `AbstractClass` | parameter 'renderer': `Renderer` is not an emitted class: required parameter 'backend' cannot be mapped: `Backend` is not an emitted class: the class is abstract, so it has no constructor to mirror |
| `QuadraticBezierCurve` | `property isQuadraticBezierCurve` | `UntypedValue` | the declaration carries no type |
| `QuadraticBezierCurve` | `property type` | `UnmappedUnion` | `string | "QuadraticBezierCurve"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `QuadraticBezierCurve` | `property v0` | `MathValueType` | `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `QuadraticBezierCurve` | `property v1` | `MathValueType` | `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `QuadraticBezierCurve` | `property v2` | `MathValueType` | `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `QuadraticBezierCurve3` | `property isQuadraticBezierCurve3` | `UntypedValue` | the declaration carries no type |
| `QuadraticBezierCurve3` | `property type` | `UnmappedUnion` | `string | "QuadraticBezierCurve3"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Quaternion` | `property isQuaternion` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `Quaternion` | `property _onChangeCallback` | `CallbackType` | `() => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `Quaternion` | `method copy` | `OptionsInterface` | parameter 'q': `QuaternionLike` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Quaternion` | `method setFromAxisAngle` | `OptionsInterface` | parameter 'axis': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Quaternion` | `method setFromUnitVectors` | `OptionsInterface` | parameter 'vTo': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Quaternion` | `method fromArray` | `UnmappedUnion` | parameter 'array': `number[] | ArrayLike<number>` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Quaternion` | `method toArray` | `CollectionType` | parameter 'array': `number[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Quaternion` | `method toJSON` | `CollectionType` | return type: `[number, number, number, number]` is a tuple, which has no wire encoding |
| `Quaternion` | `method fromBufferAttribute` | `UnmappedUnion` | parameter 'attribute': `BufferAttribute | InterleavedBufferAttribute` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Quaternion` | `method _onChange` | `CallbackType` | parameter 'callback': `() => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `Quaternion` | `method slerpFlat` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Quaternion` | `method multiplyQuaternionsFlat` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Quaternion` | `method [Symbol.iterator]` | `NotInstanceApi` | the member name is not a usable C# identifier |
| `QuaternionLinearInterpolant` | `method interpolate_` | `UnmappedTypeAlias` | return type: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `RawShaderMaterial` | `property isRawShaderMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `Ray` | `method copy` | `MathValueType` | parameter 'ray': `Ray` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Ray` | `method intersectSphere` | `MathValueType` | parameter 'sphere': `Sphere` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Ray` | `method intersectsSphere` | `MathValueType` | parameter 'sphere': `Sphere` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Ray` | `method distanceToPlane` | `MathValueType` | parameter 'plane': `Plane` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Ray` | `method intersectPlane` | `MathValueType` | parameter 'plane': `Plane` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Ray` | `method intersectsPlane` | `MathValueType` | parameter 'plane': `Plane` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Ray` | `method intersectBox` | `MathValueType` | parameter 'box': `Box3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Ray` | `method intersectsBox` | `MathValueType` | parameter 'box': `Box3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Ray` | `method equals` | `MathValueType` | parameter 'ray': `Ray` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Ray` | `method isIntersectionBox` | `UntypedValue` | parameter 'b': `any` carries no type information a C# signature could express |
| `Ray` | `method isIntersectionPlane` | `UntypedValue` | parameter 'p': `any` carries no type information a C# signature could express |
| `Ray` | `method isIntersectionSphere` | `UntypedValue` | parameter 's': `any` carries no type information a C# signature could express |
| `Raycaster` | `property ray` | `MathValueType` | `Ray` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Raycaster` | `property params` | `OptionsInterface` | `RaycasterParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `Raycaster` | `method setFromCamera` | `MathValueType` | parameter 'coords': `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Raycaster` | `method setFromXRController` | `UnwrappedClass` | parameter 'controller': `XRTargetRaySpace` is not an emitted class: renderer internals under `src/renderers/webxr/**`; no consumer instantiates them and emitting them would inflate the coverage table |
| `Raycaster` | `method intersectObject` | `DomOrLibType` | parameter 'optionalTarget': `Array<Intersection<TIntersected>>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Raycaster` | `method intersectObjects` | `CollectionType` | parameter 'objects': `Object3D[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `ReadbackBuffer` | `property buffer` | `DomOrLibType` | `ArrayBuffer` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `ReadbackBuffer` | `property isReadbackBuffer` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `RectAreaLight` | `property isRectAreaLight` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `RenderPipeline` | `property renderer` | `AbstractClass` | `Renderer` is not an emitted class: required parameter 'backend' cannot be mapped: `Backend` is not an emitted class: the class is abstract, so it has no constructor to mirror |
| `RenderPipeline` | `property outputNode` | `NodeStackType` | `Node` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `RenderPipeline` | `method renderAsync` | `DomOrLibType` | return type: `Promise<void>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `RenderTarget` | `property isRenderTarget` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `RenderTarget` | `property scissor` | `MathValueType` | `Vector4` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `RenderTarget` | `property viewport` | `MathValueType` | `Vector4` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `RenderTarget` | `property textures` | `CollectionType` | `TTexture[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `RenderTarget` | `property texture` | `ConstructorOverloads` | `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `RenderTarget3D` | `property isRenderTarget3D` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `Renderer` | `property isRenderer` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `Renderer` | `property backend` | `AbstractClass` | `Backend` is not an emitted class: the class is abstract, so it has no constructor to mirror |
| `Renderer` | `property logarithmicDepthBuffer` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `Renderer` | `property reversedDepthBuffer` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `Renderer` | `property contextNode` | `NodeStackType` | `ContextNode<unknown>` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `Renderer` | `property onDeviceLost` | `CallbackType` | `(info: DeviceLostInfo) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `Renderer` | `property onError` | `CallbackType` | `(errorMessage: string) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `Renderer` | `property _currentSourceMaterial` | `NotInstanceApi` | declared `private`, so it is not part of the public API |
| `Renderer` | `property shadowMap` | `AnonymousObjectType` | `{ /** * - Whether to globally enable shadows or not. */ enabled: boolean; /** * - Whether to enable light transmission through non-opaque materials. */ transmitted: boolean; /** * - The shadow map type. */ type: ShadowMapType; }` is an anonymous object literal type with no named C# equivalent |
| `Renderer` | `property xr` | `AbstractClass` | `XRManager` is not an emitted class: required parameter 'renderer' cannot be mapped: `Renderer` is not an emitted class: required parameter 'backend' cannot be mapped: `Backend` is not an emitted class: the class is abstract, so it has no constructor to mirror |
| `Renderer` | `property debug` | `AnonymousObjectType` | `{ /** * - Whether shader errors should be checked or not. */ checkShaderErrors: boolean; /** * - A callback function that is executed when a shader error happens. Only supported with WebGL 2 right now. */ onShaderError: | (( gl: WebGL2RenderingContext, programGPU: WebGLProgram, glVertexShader: WebGLShader, glFragmentShader: WebGLShader, ) => void) | null; /** * - Allows the get the raw shader code for the given scene, camera and 3D object. */ getShaderAsync: (scene: Scene, camera: Camera, object: Object3D) => Promise<{ fragmentShader: string | null; vertexShader: string | null; }>; }` is an anonymous object literal type with no named C# equivalent |
| `Renderer` | `property domElement` | `DomOrLibType` | `HTMLCanvasElement` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Renderer` | `property coordinateSystem` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `CoordinateSystem`), and the wire format has no read op — C# could neither write it nor observe it |
| `Renderer` | `property initialized` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `Renderer` | `property needsFrameBufferTarget` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `Renderer` | `property samples` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `Renderer` | `property currentSamples` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `Renderer` | `property currentToneMapping` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `ToneMapping`), and the wire format has no read op — C# could neither write it nor observe it |
| `Renderer` | `property currentColorSpace` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `string`), and the wire format has no read op — C# could neither write it nor observe it |
| `Renderer` | `property isOutputTarget` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `Renderer` | `property compile` | `CallbackType` | `(scene: Object3D, camera: Camera, targetScene?: Scene | null) => Promise<void>` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `Renderer` | `method init` | `DomOrLibType` | return type: `Promise<this>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Renderer` | `method compileAsync` | `DomOrLibType` | return type: `Promise<void>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Renderer` | `method renderAsync` | `DomOrLibType` | return type: `Promise<void>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Renderer` | `method waitForGPU` | `DomOrLibType` | return type: `Promise<void>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Renderer` | `method setMRT` | `NodeStackType` | parameter 'mrt': `MRTNode` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `Renderer` | `method getMRT` | `NodeStackType` | return type: `MRTNode` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `Renderer` | `method setAnimationLoop` | `CallbackType` | parameter 'callback': `(time: DOMHighResTimeStamp, frame?: XRFrame) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `Renderer` | `method getAnimationLoop` | `CallbackType` | return type: `(time: DOMHighResTimeStamp, xrFrame?: XRFrame) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `Renderer` | `method getArrayBufferAsync` | `UnmappedTypeAlias` | parameter 'attribute': `BufferAttribute` is not an emitted class: required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Renderer` | `method getContext` | `UntypedValue` | return type: `unknown` carries no type information a C# signature could express |
| `Renderer` | `method getDrawingBufferSize` | `MathValueType` | parameter 'target': `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Renderer` | `method getSize` | `MathValueType` | parameter 'target': `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Renderer` | `method setOpaqueSort` | `CallbackType` | parameter 'method': `(a: RenderItem, b: RenderItem) => number` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `Renderer` | `method setTransparentSort` | `CallbackType` | parameter 'method': `(a: RenderItem, b: RenderItem) => number` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `Renderer` | `method getScissor` | `MathValueType` | parameter 'target': `Vector4` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Renderer` | `method setScissor` | `MathValueType` | parameter 'x': `Vector4` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Renderer` | `method getViewport` | `MathValueType` | parameter 'target': `Vector4` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Renderer` | `method setViewport` | `MathValueType` | parameter 'x': `Vector4` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Renderer` | `method clearAsync` | `DomOrLibType` | return type: `Promise<void>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Renderer` | `method clearColorAsync` | `DomOrLibType` | return type: `Promise<void>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Renderer` | `method clearDepthAsync` | `DomOrLibType` | return type: `Promise<void>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Renderer` | `method clearStencilAsync` | `DomOrLibType` | return type: `Promise<void>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Renderer` | `method setCanvasTarget` | `UnmappedUnion` | parameter 'canvasTarget': `CanvasTarget` is not an emitted class: required parameter 'domElement' cannot be mapped: `HTMLCanvasElement | OffscreenCanvas` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Renderer` | `method getCanvasTarget` | `UnmappedUnion` | return type: `CanvasTarget` is not an emitted class: required parameter 'domElement' cannot be mapped: `HTMLCanvasElement | OffscreenCanvas` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Renderer` | `method setRenderObjectFunction` | `CallbackType` | parameter 'renderObjectFunction': `( object: Object3D, scene: Scene, camera: Camera, geometry: BufferGeometry, material: Material, group: GeometryGroup | null, lightsNode: LightsNode, clippingContext: ClippingContext, passId?: string | null | undefined, ) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `Renderer` | `method getRenderObjectFunction` | `CallbackType` | return type: `( object: Object3D, scene: Scene, camera: Camera, geometry: BufferGeometry, material: Material, group: GeometryGroup | null, lightsNode: LightsNode, clippingContext: ClippingContext, passId?: string | null | undefined, ) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `Renderer` | `method compute` | `UnmappedUnion` | parameter 'computeNodes': `ComputeNode | ComputeNode[]` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Renderer` | `method computeAsync` | `UnmappedUnion` | parameter 'computeNodes': `ComputeNode | ComputeNode[]` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Renderer` | `method hasFeatureAsync` | `DomOrLibType` | return type: `Promise<boolean>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Renderer` | `method resolveTimestampsAsync` | `UnmappedTypeAlias` | parameter 'type': `TimestampQuery` aliases `typeof TimestampQuery.COMPUTE | typeof TimestampQuery.RENDER`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Renderer` | `method initTextureAsync` | `ConstructorOverloads` | parameter 'texture': `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `Renderer` | `method initTexture` | `ConstructorOverloads` | parameter 'texture': `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `Renderer` | `method copyFramebufferToTexture` | `UnmappedUnion` | parameter 'rectangle': `Vector2 | Vector4` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Renderer` | `method copyTextureToTexture` | `ConstructorOverloads` | parameter 'srcTexture': `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `Renderer` | `method readRenderTargetPixelsAsync` | `DomOrLibType` | return type: `Promise<TypedArray>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Renderer` | `method renderObject` | `OptionsInterface` | parameter 'group': `GeometryGroup` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `RingGeometry` | `property type` | `UnmappedUnion` | `string | "RingGeometry"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `RingGeometry` | `property parameters` | `AnonymousObjectType` | `{ readonly innerRadius: number; readonly outerRadius: number; readonly thetaSegments: number; readonly phiSegments: number; readonly thetaStart: number; readonly thetaLength: number; }` is an anonymous object literal type with no named C# equivalent |
| `RingGeometry` | `method fromJSON` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `SSSLightingModel` | `method direct` | `NodeStackType` | parameter 'input': `LightingModelDirectInput` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `Scene` | `property isScene` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `Scene` | `property background` | `UnmappedUnion` | `Color | Texture` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Scene` | `property environment` | `ConstructorOverloads` | `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `Scene` | `property fog` | `UnmappedUnion` | `Fog | FogExp2` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Scene` | `method toJSON` | `OptionsInterface` | parameter 'meta': `JSONMeta` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Scene` | `property environmentNode` | `NodeStackType` | `Node<"vec3">` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `Scene` | `property backgroundNode` | `NodeStackType` | `Node` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `Scene` | `property fogNode` | `NodeStackType` | `Node` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `ShaderMaterial` | `property isShaderMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `ShaderMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `ShaderMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `ShaderMaterial` | `method toJSON` | `OptionsInterface` | parameter 'meta': `JSONMeta` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `ShaderMaterial` | `method fromJSON` | `OptionsInterface` | parameter 'json': `MaterialJSON` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `ShadowMaterial` | `property isShadowMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `ShadowMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `ShadowMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `ShadowNodeMaterial` | `property isShadowNodeMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `ShadowNodeMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `ShadowNodeMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `ShadowNodeMaterial` | `method setupLightingModel` | `NodeStackType` | return type: `ShadowMaskModel` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `Shape` | `property type` | `UnmappedUnion` | `string | "Shape"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Shape` | `property holes` | `CollectionType` | `Path[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Shape` | `method extractPoints` | `AnonymousObjectType` | return type: `{ shape: Vector2[]; holes: Vector2[][]; }` is an anonymous object literal type with no named C# equivalent |
| `Shape` | `method getPointsHoles` | `CollectionType` | return type: `Vector2[][]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Shape` | `method toJSON` | `OptionsInterface` | return type: `ShapeJSON` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Shape` | `method fromJSON` | `OptionsInterface` | parameter 'json': `ShapeJSON` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `ShapeGeometry` | `property type` | `UnmappedUnion` | `string | "ShapeGeometry"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `ShapeGeometry` | `property parameters` | `AnonymousObjectType` | `{ readonly shapes: Shape | Shape[]; readonly curveSegments: number; }` is an anonymous object literal type with no named C# equivalent |
| `ShapeGeometry` | `method fromJSON` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `ShapePath` | `property subPaths` | `DomOrLibType` | `Array<Path>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `ShapePath` | `property userData` | `DomOrLibType` | `Record<string, unknown>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `ShapePath` | `method splineThru` | `DomOrLibType` | parameter 'pts': `Array<Vector2>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `ShapePath` | `method toShapes` | `DomOrLibType` | return type: `Array<Shape>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `ShapeUtils` | `method area` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `ShapeUtils` | `method isClockWise` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `ShapeUtils` | `method triangulateShape` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Skeleton` | `property bones` | `CollectionType` | `Bone[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Skeleton` | `property boneInverses` | `CollectionType` | `Matrix4[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Skeleton` | `property boneMatrices` | `DomOrLibType` | `Float32Array` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Skeleton` | `method toJSON` | `OptionsInterface` | return type: `SkeletonJSON` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Skeleton` | `method fromJSON` | `OptionsInterface` | parameter 'json': `SkeletonJSON` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `SkeletonHelper` | `property isSkeletonHelper` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `SkeletonHelper` | `property bones` | `CollectionType` | `Bone[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `SkinnedMesh` | `property isSkinnedMesh` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `SkinnedMesh` | `property type` | `UnmappedUnion` | `string | "SkinnedMesh"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `SkinnedMesh` | `property bindMode` | `StringConstantGroup` | `BindMode` cannot become a C# enum: the group is string-valued (`AttachedBindMode` = "attached"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `SkinnedMesh` | `property boundingBox` | `MathValueType` | `Box3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `SkinnedMesh` | `property boundingSphere` | `MathValueType` | `Sphere` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `SkinnedMesh` | `method toJSON` | `OptionsInterface` | parameter 'meta': `JSONMeta` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Source` | `property isSource` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `Source` | `property id` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `Source` | `property data` | `UnerasableTypeParameter` | type parameter `TData` has neither a default nor a constraint, so erasing it leaves nothing to map to |
| `Source` | `method toJSON` | `UnmappedUnion` | parameter 'meta': `string | {}` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `SourceJSON` | `property url` | `UnmappedUnion` | `SerializedImage | SerializedImage[]` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Sphere` | `property isSphere` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `Sphere` | `method setFromPoints` | `CollectionType` | parameter 'points': `Vector3[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Sphere` | `method copy` | `MathValueType` | parameter 'sphere': `Sphere` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Sphere` | `method intersectsSphere` | `MathValueType` | parameter 'sphere': `Sphere` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Sphere` | `method intersectsBox` | `MathValueType` | parameter 'box': `Box3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Sphere` | `method intersectsPlane` | `MathValueType` | parameter 'plane': `Plane` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Sphere` | `method getBoundingBox` | `MathValueType` | parameter 'target': `Box3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Sphere` | `method equals` | `MathValueType` | parameter 'sphere': `Sphere` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Sphere` | `method union` | `MathValueType` | parameter 'sphere': `Sphere` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Sphere` | `method empty` | `UntypedValue` | return type: `any` carries no type information a C# signature could express |
| `Sphere` | `method toJSON` | `OptionsInterface` | return type: `SphereJSON` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Sphere` | `method fromJSON` | `OptionsInterface` | parameter 'json': `SphereJSON` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `SphereGeometry` | `property type` | `UnmappedUnion` | `string | "SphereGeometry"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `SphereGeometry` | `property parameters` | `AnonymousObjectType` | `{ readonly radius: number; readonly widthSegments: number; readonly heightSegments: number; readonly phiStart: number; readonly phiLength: number; readonly thetaStart: number; readonly thetaLength: number; }` is an anonymous object literal type with no named C# equivalent |
| `SphereGeometry` | `method fromJSON` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Spherical` | `method copy` | `MathValueType` | parameter 'other': `Spherical` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `SphericalHarmonics3` | `property coefficients` | `CollectionType` | `Vector3[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `SphericalHarmonics3` | `property isSphericalHarmonics3` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `SphericalHarmonics3` | `method set` | `CollectionType` | parameter 'coefficients': `Vector3[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `SphericalHarmonics3` | `method add` | `MathValueType` | parameter 'sh': `SphericalHarmonics3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `SphericalHarmonics3` | `method addScaledSH` | `MathValueType` | parameter 'sh': `SphericalHarmonics3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `SphericalHarmonics3` | `method lerp` | `MathValueType` | parameter 'sh': `SphericalHarmonics3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `SphericalHarmonics3` | `method equals` | `MathValueType` | parameter 'sh': `SphericalHarmonics3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `SphericalHarmonics3` | `method copy` | `MathValueType` | parameter 'sh': `SphericalHarmonics3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `SphericalHarmonics3` | `method fromArray` | `UnmappedUnion` | parameter 'array': `number[] | ArrayLike<number>` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `SphericalHarmonics3` | `method toArray` | `CollectionType` | parameter 'array': `number[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `SphericalHarmonics3` | `method getBasisAt` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `SplineCurve` | `property isSplineCurve` | `UntypedValue` | the declaration carries no type |
| `SplineCurve` | `property type` | `UnmappedUnion` | `string | "SplineCurve"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `SplineCurve` | `property points` | `CollectionType` | `Vector2[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `SpotLight` | `property isSpotLight` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `SpotLight` | `property map` | `ConstructorOverloads` | `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `SpotLight` | `method toJSON` | `OptionsInterface` | parameter 'meta': `JSONMeta` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `SpotLightShadow` | `property isSpotLightShadow` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `Sprite` | `property isSprite` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `Sprite` | `property type` | `UnmappedUnion` | `string | "Sprite"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Sprite` | `property castShadow` | `LiteralType` | `false` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `Sprite` | `property center` | `MathValueType` | `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `SpriteMaterial` | `property isSpriteMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `SpriteMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `SpriteMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `SpriteNodeMaterial` | `property isSpriteMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `SpriteNodeMaterial` | `property isSpriteNodeMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `SpriteNodeMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `SpriteNodeMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `SpriteNodeMaterial` | `method setupPositionView` | `NodeStackType` | parameter 'builder': `NodeBuilder` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `StereoCamera` | `property type` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `string`), and the wire format has no read op — C# could neither write it nor observe it |
| `Storage3DTexture` | `property isStorageTexture` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `Storage3DTexture` | `property isStorageTexture` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `StorageBufferAttribute` | `property isStorageBufferAttribute` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `StorageInstancedBufferAttribute` | `property isStorageInstancedBufferAttribute` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `TetrahedronGeometry` | `property type` | `UnmappedUnion` | `string | "TetrahedronGeometry"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `TetrahedronGeometry` | `method fromJSON` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Texture` | `property isTexture` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `Texture` | `property id` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `Texture` | `property source` | `UnerasableTypeParameter` | `Source<TImage>` is not an emitted class: required parameter 'data' cannot be mapped: type parameter `TData` has neither a default nor a constraint, so erasing it leaves nothing to map to |
| `Texture` | `property width` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `Texture` | `property height` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `Texture` | `property depth` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number`), and the wire format has no read op — C# could neither write it nor observe it |
| `Texture` | `property image` | `UntypedValue` | `unknown` carries no type information a C# signature could express |
| `Texture` | `property mipmaps` | `UnmappedUnion` | `CompressedTextureMipmap[] | CubeTexture[] | HTMLCanvasElement[]` unions 3 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Texture` | `property mapping` | `UnmappedTypeAlias` | `AnyMapping` aliases `Mapping | CubeTextureMapping`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Texture` | `property format` | `UnmappedTypeAlias` | `AnyPixelFormat` aliases `PixelFormat | DepthTexturePixelFormat | CompressedPixelFormat`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Texture` | `property internalFormat` | `UnmappedTypeAlias` | `PixelFormatGPU` aliases `| "ALPHA" | "RGB" | "RGBA" | "LUMINANCE" | "LUMINANCE_ALPHA" | "RED_INTEGER" | "R8" | "R8_SNORM" | "R8I" | "R8UI" | "R16I" | "R16UI" | "R16F" | "R32I" | "R32UI" | "R32F" | "RG8" | "RG8_SNORM" | "RG8I" | "RG8UI" | "RG16I" | "RG16UI" | "RG16F" | "RG32I" | "RG32UI" | "RG32F" | "RGB565" | "RGB8" | "RGB8_SNORM" | "RGB8I" | "RGB8UI" | "RGB16I" | "RGB16UI" | "RGB16F" | "RGB32I" | "RGB32UI" | "RGB32F" | "RGB9_E5" | "SRGB8" | "R11F_G11F_B10F" | "RGBA4" | "RGBA8" | "RGBA8_SNORM" | "RGBA8I" | "RGBA8UI" | "RGBA16I" | "RGBA16UI" | "RGBA16F" | "RGBA32I" | "RGBA32UI" | "RGBA32F" | "RGB5_A1" | "RGB10_A2" | "RGB10_A2UI" | "SRGB8_ALPHA8" | "SRGB8" | "DEPTH_COMPONENT16" | "DEPTH_COMPONENT24" | "DEPTH_COMPONENT32F" | "DEPTH24_STENCIL8" | "DEPTH32F_STENCIL8"`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Texture` | `property matrix` | `MathValueType` | `Matrix3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Texture` | `property offset` | `MathValueType` | `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Texture` | `property repeat` | `MathValueType` | `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Texture` | `property center` | `MathValueType` | `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Texture` | `property userData` | `DomOrLibType` | `Record<string, any>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Texture` | `property updateRanges` | `DomOrLibType` | `Array<{ start: number; count: number }>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `Texture` | `property DEFAULT_ANISOTROPY` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Texture` | `property DEFAULT_IMAGE` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Texture` | `property DEFAULT_MAPPING` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Texture` | `property onUpdate` | `CallbackType` | `(texture: Texture) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `Texture` | `method transformUv` | `MathValueType` | parameter 'uv': `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Texture` | `method copy` | `ConstructorOverloads` | parameter 'source': `Texture<TImage>` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `Texture` | `method setValues` | `OptionsInterface` | parameter 'values': `TextureParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `Texture` | `method toJSON` | `UnmappedUnion` | parameter 'meta': `string | {}` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `TextureLoader` | `method load` | `CallbackType` | parameter 'onLoad': `(data: Texture<HTMLImageElement>) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `Timer` | `method connect` | `DomOrLibType` | parameter 'document': `Document` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `TimestampQueryPool` | `property queryOffsets` | `DomOrLibType` | `Map<string, number>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `TimestampQueryPool` | `property frames` | `CollectionType` | `number[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `TimestampQueryPool` | `property pendingResolve` | `UnmappedUnion` | `boolean | Promise<number>` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `TimestampQueryPool` | `property timestamps` | `DomOrLibType` | `Map<string, number>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `TimestampQueryPool` | `method getTimestampFrames` | `CollectionType` | return type: `number[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `TimestampQueryPool` | `method allocateQueriesForContext` | `NotInstanceApi` | abstract, so there is no implementation to mirror |
| `TimestampQueryPool` | `method resolveQueriesAsync` | `NotInstanceApi` | abstract, so there is no implementation to mirror |
| `TimestampQueryPool` | `method dispose` | `NotInstanceApi` | abstract, so there is no implementation to mirror |
| `TorusGeometry` | `property type` | `UnmappedUnion` | `string | "TorusGeometry"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `TorusGeometry` | `property parameters` | `AnonymousObjectType` | `{ readonly radius: number; readonly tube: number; readonly radialSegments: number; readonly tubularSegments: number; readonly arc: number; readonly thetaStart: number; readonly thetaLength: number; }` is an anonymous object literal type with no named C# equivalent |
| `TorusGeometry` | `method fromJSON` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `TorusKnotGeometry` | `property type` | `UnmappedUnion` | `string | "TorusKnotGeometry"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `TorusKnotGeometry` | `property parameters` | `AnonymousObjectType` | `{ readonly radius: number; readonly tube: number; readonly tubularSegments: number; readonly radialSegments: number; readonly p: number; readonly q: number; }` is an anonymous object literal type with no named C# equivalent |
| `TorusKnotGeometry` | `method fromJSON` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Triangle` | `method setFromPointsAndIndices` | `CollectionType` | parameter 'points': `Vector3[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Triangle` | `method setFromAttributeAndIndices` | `UnmappedUnion` | parameter 'attribute': `BufferAttribute | InterleavedBufferAttribute` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Triangle` | `method copy` | `MathValueType` | parameter 'triangle': `Triangle` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Triangle` | `method getPlane` | `MathValueType` | parameter 'target': `Plane` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Triangle` | `method getInterpolation` | `MathValueType` | parameter 'v1': `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Triangle` | `method intersectsBox` | `MathValueType` | parameter 'box': `Box3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Triangle` | `method equals` | `MathValueType` | parameter 'triangle': `Triangle` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Triangle` | `method getNormal` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Triangle` | `method getBarycoord` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Triangle` | `method containsPoint` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Triangle` | `method getInterpolation` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Triangle` | `method getInterpolatedAttribute` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Triangle` | `method isFrontFacing` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `TubeGeometry` | `property type` | `UnmappedUnion` | `string | "TubeGeometry"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `TubeGeometry` | `property parameters` | `AnonymousObjectType` | `{ readonly path: Curve<Vector3>; readonly tubularSegments: number; readonly radius: number; readonly radialSegments: number; readonly closed: boolean; }` is an anonymous object literal type with no named C# equivalent |
| `TubeGeometry` | `property tangents` | `CollectionType` | `Vector3[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `TubeGeometry` | `property normals` | `CollectionType` | `Vector3[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `TubeGeometry` | `property binormals` | `CollectionType` | `Vector3[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `TubeGeometry` | `method fromJSON` | `NotInstanceApi` | static; the mirror models instances, and a static write has no handle to address |
| `Uniform` | `property value` | `UntypedValue` | `any` carries no type information a C# signature could express |
| `UniformsGroup` | `property isUniformsGroup` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `UniformsGroup` | `property uniforms` | `DomOrLibType` | `Array<Uniform | Uniform[]>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `UniformsGroup` | `method add` | `UnmappedUnion` | parameter 'uniform': `Uniform | Uniform[]` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `UniformsGroup` | `method remove` | `UnmappedUnion` | parameter 'uniform': `Uniform | Uniform[]` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Vector2` | `property isVector2` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `Vector2` | `method copy` | `OptionsInterface` | parameter 'v': `Vector2Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector2` | `method add` | `OptionsInterface` | parameter 'v': `Vector2Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector2` | `method addVectors` | `OptionsInterface` | parameter 'a': `Vector2Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector2` | `method addScaledVector` | `OptionsInterface` | parameter 'v': `Vector2Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector2` | `method sub` | `OptionsInterface` | parameter 'v': `Vector2Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector2` | `method subVectors` | `OptionsInterface` | parameter 'a': `Vector2Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector2` | `method multiply` | `OptionsInterface` | parameter 'v': `Vector2Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector2` | `method divide` | `OptionsInterface` | parameter 'v': `Vector2Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector2` | `method applyMatrix3` | `MathValueType` | parameter 'm': `Matrix3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Vector2` | `method min` | `OptionsInterface` | parameter 'v': `Vector2Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector2` | `method max` | `OptionsInterface` | parameter 'v': `Vector2Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector2` | `method clamp` | `OptionsInterface` | parameter 'min': `Vector2Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector2` | `method dot` | `OptionsInterface` | parameter 'v': `Vector2Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector2` | `method cross` | `OptionsInterface` | parameter 'v': `Vector2Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector2` | `method angleTo` | `MathValueType` | parameter 'v': `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Vector2` | `method distanceTo` | `OptionsInterface` | parameter 'v': `Vector2Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector2` | `method distanceToSquared` | `OptionsInterface` | parameter 'v': `Vector2Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector2` | `method manhattanDistanceTo` | `OptionsInterface` | parameter 'v': `Vector2Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector2` | `method lerp` | `OptionsInterface` | parameter 'v': `Vector2Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector2` | `method lerpVectors` | `OptionsInterface` | parameter 'v1': `Vector2Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector2` | `method equals` | `OptionsInterface` | parameter 'v': `Vector2Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector2` | `method fromArray` | `UnmappedUnion` | parameter 'array': `number[] | ArrayLike<number>` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Vector2` | `method toArray` | `CollectionType` | parameter 'array': `number[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Vector2` | `method fromBufferAttribute` | `UnmappedTypeAlias` | parameter 'attribute': `BufferAttribute` is not an emitted class: required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Vector2` | `method rotateAround` | `OptionsInterface` | parameter 'center': `Vector2Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector2` | `method [Symbol.iterator]` | `NotInstanceApi` | the member name is not a usable C# identifier |
| `Vector3` | `property isVector3` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `Vector3` | `method copy` | `OptionsInterface` | parameter 'v': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method add` | `OptionsInterface` | parameter 'v': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method addVectors` | `OptionsInterface` | parameter 'a': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method addScaledVector` | `OptionsInterface` | parameter 'v': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method sub` | `OptionsInterface` | parameter 'v': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method subVectors` | `OptionsInterface` | parameter 'a': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method multiply` | `OptionsInterface` | parameter 'v': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method multiplyVectors` | `OptionsInterface` | parameter 'a': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method applyAxisAngle` | `OptionsInterface` | parameter 'axis': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method applyMatrix3` | `MathValueType` | parameter 'm': `Matrix3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Vector3` | `method applyNormalMatrix` | `MathValueType` | parameter 'm': `Matrix3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Vector3` | `method applyQuaternion` | `OptionsInterface` | parameter 'q': `QuaternionLike` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method divide` | `OptionsInterface` | parameter 'v': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method min` | `OptionsInterface` | parameter 'v': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method max` | `OptionsInterface` | parameter 'v': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method clamp` | `OptionsInterface` | parameter 'min': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method dot` | `OptionsInterface` | parameter 'v': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method lerp` | `OptionsInterface` | parameter 'v': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method lerpVectors` | `OptionsInterface` | parameter 'v1': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method cross` | `OptionsInterface` | parameter 'v': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method crossVectors` | `OptionsInterface` | parameter 'a': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method reflect` | `OptionsInterface` | parameter 'normal': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method distanceTo` | `OptionsInterface` | parameter 'v': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method distanceToSquared` | `OptionsInterface` | parameter 'v': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method manhattanDistanceTo` | `OptionsInterface` | parameter 'v': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method setFromSpherical` | `MathValueType` | parameter 's': `Spherical` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Vector3` | `method setFromCylindrical` | `MathValueType` | parameter 's': `Cylindrical` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Vector3` | `method setFromMatrix3Column` | `MathValueType` | parameter 'matrix': `Matrix3` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `Vector3` | `method setFromColor` | `OptionsInterface` | parameter 'color': `RGB` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method equals` | `OptionsInterface` | parameter 'v': `Vector3Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector3` | `method fromArray` | `UnmappedUnion` | parameter 'array': `number[] | ArrayLike<number>` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Vector3` | `method toArray` | `CollectionType` | parameter 'array': `number[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Vector3` | `method fromBufferAttribute` | `UnmappedUnion` | parameter 'attribute': `BufferAttribute | InterleavedBufferAttribute` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Vector3` | `method [Symbol.iterator]` | `NotInstanceApi` | the member name is not a usable C# identifier |
| `Vector4` | `property isVector4` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `Vector4` | `method copy` | `OptionsInterface` | parameter 'v': `Vector4Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector4` | `method add` | `OptionsInterface` | parameter 'v': `Vector4Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector4` | `method addVectors` | `OptionsInterface` | parameter 'a': `Vector4Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector4` | `method addScaledVector` | `OptionsInterface` | parameter 'v': `Vector4Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector4` | `method sub` | `OptionsInterface` | parameter 'v': `Vector4Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector4` | `method subVectors` | `OptionsInterface` | parameter 'a': `Vector4Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector4` | `method multiply` | `OptionsInterface` | parameter 'v': `Vector4Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector4` | `method divide` | `OptionsInterface` | parameter 'v': `Vector4Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector4` | `method setAxisAngleFromQuaternion` | `OptionsInterface` | parameter 'q': `QuaternionLike` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector4` | `method min` | `OptionsInterface` | parameter 'v': `Vector4Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector4` | `method max` | `OptionsInterface` | parameter 'v': `Vector4Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector4` | `method clamp` | `OptionsInterface` | parameter 'min': `Vector4Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector4` | `method dot` | `OptionsInterface` | parameter 'v': `Vector4Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector4` | `method lerp` | `OptionsInterface` | parameter 'v': `Vector4Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector4` | `method lerpVectors` | `OptionsInterface` | parameter 'v1': `Vector4Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector4` | `method equals` | `OptionsInterface` | parameter 'v': `Vector4Like` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `Vector4` | `method fromArray` | `UnmappedUnion` | parameter 'array': `number[] | ArrayLike<number>` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `Vector4` | `method toArray` | `CollectionType` | parameter 'array': `number[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `Vector4` | `method fromBufferAttribute` | `UnmappedTypeAlias` | parameter 'attribute': `BufferAttribute` is not an emitted class: required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `Vector4` | `method [Symbol.iterator]` | `NotInstanceApi` | the member name is not a usable C# identifier |
| `VideoFrameTexture` | `method setFrame` | `UntypedValue` | parameter 'frame': `unknown` carries no type information a C# signature could express |
| `VideoTexture` | `property isVideoTexture` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `VolumeNodeMaterial` | `property isVolumeNodeMaterial` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `VolumeNodeMaterial` | `method setValues` | `OptionsInterface` | parameter 'values': `VolumeNodeMaterialParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `VolumeNodeMaterial` | `method setupLightingModel` | `NodeStackType` | return type: `VolumetricLightingModel` is declared under `src/nodes/**`, the TSL / WebGPU node stack that is outside the extracted API surface |
| `WebGL3DRenderTarget` | `property textures` | `CollectionType` | `Data3DTexture[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `WebGL3DRenderTarget` | `property isWebGL3DRenderTarget` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `WebGLArrayRenderTarget` | `property textures` | `CollectionType` | `DataArrayTexture[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `WebGLArrayRenderTarget` | `property isWebGLArrayRenderTarget` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `WebGLAttributes` | `method get` | `UnmappedUnion` | parameter 'attribute': `BufferAttribute | InterleavedBufferAttribute | GLBufferAttribute` unions 3 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `WebGLAttributes` | `method remove` | `UnmappedUnion` | parameter 'attribute': `BufferAttribute | InterleavedBufferAttribute | GLBufferAttribute` unions 3 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `WebGLAttributes` | `method update` | `UnmappedUnion` | parameter 'attribute': `BufferAttribute | InterleavedBufferAttribute | GLBufferAttribute` unions 3 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `WebGLBackend` | `property isWebGLBackend` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `WebGLBackend` | `property coordinateSystem` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `CoordinateSystem`), and the wire format has no read op — C# could neither write it nor observe it |
| `WebGLBindingStates` | `method setup` | `UnwrappedClass` | parameter 'program': `WebGLProgram` is not an emitted class: renderer internals under `src/renderers/webgl/**`; no consumer instantiates them and emitting them would inflate the coverage table |
| `WebGLBufferRenderer` | `property setMode` | `CallbackType` | `(value: any) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebGLBufferRenderer` | `property render` | `CallbackType` | `(start: any, count: number) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebGLBufferRenderer` | `property renderInstances` | `CallbackType` | `(start: any, count: number, primcount: number) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebGLBufferRenderer` | `property renderMultiDraw` | `CallbackType` | `(starts: Int32Array, counts: Int32Array, drawCount: number) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebGLCapabilities` | `property isWebGL2` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `WebGLCapabilities` | `property getMaxAnisotropy` | `CallbackType` | `() => number` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebGLCapabilities` | `property getMaxPrecision` | `CallbackType` | `(precision: string) => string` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebGLCapabilities` | `property textureFormatReadable` | `CallbackType` | `(textureFormat: PixelFormat) => boolean` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebGLCapabilities` | `property textureTypeReadable` | `CallbackType` | `(textureType: TextureDataType) => boolean` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebGLClipping` | `property uniform` | `AnonymousObjectType` | `{ value: any; needsUpdate: boolean }` is an anonymous object literal type with no named C# equivalent |
| `WebGLClipping` | `method init` | `CollectionType` | parameter 'planes': `any[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `WebGLClipping` | `method setGlobalState` | `CollectionType` | parameter 'planes': `Plane[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `WebGLCubeRenderTarget` | `property textures` | `CollectionType` | `CubeTexture[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `WebGLCubeRenderTarget` | `method fromEquirectangularTexture` | `ConstructorOverloads` | parameter 'texture': `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `WebGLEnvironments` | `method get` | `ConstructorOverloads` | parameter 'texture': `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `WebGLExtensions` | `method get` | `UntypedValue` | return type: `unknown` carries no type information a C# signature could express |
| `WebGLGeometries` | `method getWireframeAttribute` | `UnmappedTypeAlias` | return type: `BufferAttribute` is not an emitted class: required parameter 'array' cannot be mapped: `TypedArray` aliases `| Int8Array | Uint8Array | Uint8ClampedArray | Int16Array | Uint16Array | Int32Array | Uint32Array | Float32Array | Float64Array`, which is neither a group of numeric constants nor a type the mirror expresses |
| `WebGLIndexedBufferRenderer` | `property setMode` | `CallbackType` | `(value: number) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebGLIndexedBufferRenderer` | `property setIndex` | `CallbackType` | `(value: WebGLAttribute) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebGLIndexedBufferRenderer` | `property render` | `CallbackType` | `(start: number, count: number) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebGLIndexedBufferRenderer` | `property renderInstances` | `CallbackType` | `(start: number, count: number, primcount: number) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebGLIndexedBufferRenderer` | `property renderMultiDraw` | `CallbackType` | `(starts: Int32Array, counts: Int32Array, drawCount: number) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebGLInfo` | `property memory` | `AnonymousObjectType` | `{ geometries: number; textures: number; }` is an anonymous object literal type with no named C# equivalent |
| `WebGLInfo` | `property programs` | `CollectionType` | `WebGLProgram[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `WebGLInfo` | `property render` | `AnonymousObjectType` | `{ calls: number; frame: number; lines: number; points: number; triangles: number; }` is an anonymous object literal type with no named C# equivalent |
| `WebGLLights` | `property state` | `OptionsInterface` | `WebGLLightsState` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `WebGLLights` | `method setup` | `CollectionType` | parameter 'lights': `Light[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `WebGLLights` | `method setupView` | `CollectionType` | parameter 'lights': `Light[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `WebGLProgram` | `property program` | `UntypedValue` | `unknown` carries no type information a C# signature could express |
| `WebGLProgram` | `property vertexShader` | `DomOrLibType` | `WebGLShader` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `WebGLProgram` | `property fragmentShader` | `DomOrLibType` | `WebGLShader` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `WebGLProgram` | `method getUniforms` | `UnwrappedClass` | return type: `WebGLUniforms` is not an emitted class: renderer internals under `src/renderers/webgl/**`; no consumer instantiates them and emitting them would inflate the coverage table |
| `WebGLProgram` | `method getAttributes` | `UntypedValue` | return type: `unknown` carries no type information a C# signature could express |
| `WebGLPrograms` | `property programs` | `CollectionType` | `WebGLProgram[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `WebGLPrograms` | `method getParameters` | `OptionsInterface` | parameter 'lights': `WebGLLightsState` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `WebGLPrograms` | `method getProgramCacheKey` | `OptionsInterface` | parameter 'parameters': `WebGLProgramParameters` is an options bag. Every field it carries is also a settable property on the constructed object, so the mirror expresses them as properties rather than as one anonymous constructor argument |
| `WebGLPrograms` | `method getUniforms` | `AnonymousObjectType` | return type: `{ [uniform: string]: IUniform }` is an anonymous object literal type with no named C# equivalent |
| `WebGLPrograms` | `method acquireProgram` | `OptionsInterface` | parameter 'parameters': `WebGLProgramParametersWithUniforms` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `WebGLPrograms` | `method releaseProgram` | `UnwrappedClass` | parameter 'program': `WebGLProgram` is not an emitted class: renderer internals under `src/renderers/webgl/**`; no consumer instantiates them and emitting them would inflate the coverage table |
| `WebGLProperties` | `property has` | `CallbackType` | `(object: unknown) => boolean` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebGLProperties` | `property get` | `CallbackType` | `(object: unknown) => unknown` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebGLProperties` | `property remove` | `CallbackType` | `(object: unknown) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebGLProperties` | `property update` | `CallbackType` | `(object: unknown, key: unknown, value: unknown) => unknown` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebGLProperties` | `property dispose` | `CallbackType` | `() => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebGLRenderList` | `property opaque` | `CollectionType` | `RenderItem[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `WebGLRenderList` | `property transparent` | `CollectionType` | `RenderItem[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `WebGLRenderList` | `property transmissive` | `CollectionType` | `RenderItem[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `WebGLRenderList` | `method sort` | `CallbackType` | parameter 'opaqueSort': `(a: any, b: any) => number` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebGLRenderLists` | `method get` | `UnwrappedClass` | return type: `WebGLRenderList` is not an emitted class: renderer internals under `src/renderers/webgl/**`; no consumer instantiates them and emitting them would inflate the coverage table |
| `WebGLRenderTarget` | `property isWebGLRenderTarget` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `WebGLRenderer` | `property domElement` | `DomOrLibType` | `HTMLCanvasElement` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `WebGLRenderer` | `property debug` | `OptionsInterface` | `WebGLDebug` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `WebGLRenderer` | `property clippingPlanes` | `CollectionType` | `Plane[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `WebGLRenderer` | `property extensions` | `UnwrappedClass` | `WebGLExtensions` is not an emitted class: renderer internals under `src/renderers/webgl/**`; no consumer instantiates them and emitting them would inflate the coverage table |
| `WebGLRenderer` | `property coordinateSystem` | `UnmappedTypeSyntax` | `typeof WebGLCoordinateSystem` is a TypeScript `typeQuery` type, which has no C# equivalent |
| `WebGLRenderer` | `property info` | `UnwrappedClass` | `WebGLInfo` is not an emitted class: renderer internals under `src/renderers/webgl/**`; no consumer instantiates them and emitting them would inflate the coverage table |
| `WebGLRenderer` | `property shadowMap` | `UnwrappedClass` | `WebGLShadowMap` is not an emitted class: renderer internals under `src/renderers/webgl/**`; no consumer instantiates them and emitting them would inflate the coverage table |
| `WebGLRenderer` | `property properties` | `UnwrappedClass` | `WebGLProperties` is not an emitted class: renderer internals under `src/renderers/webgl/**`; no consumer instantiates them and emitting them would inflate the coverage table |
| `WebGLRenderer` | `property renderLists` | `UnwrappedClass` | `WebGLRenderLists` is not an emitted class: renderer internals under `src/renderers/webgl/**`; no consumer instantiates them and emitting them would inflate the coverage table |
| `WebGLRenderer` | `property state` | `UnwrappedClass` | `WebGLState` is not an emitted class: renderer internals under `src/renderers/webgl/**`; no consumer instantiates them and emitting them would inflate the coverage table |
| `WebGLRenderer` | `property xr` | `UnwrappedClass` | `WebXRManager` is not an emitted class: renderer internals under `src/renderers/webxr/**`; no consumer instantiates them and emitting them would inflate the coverage table |
| `WebGLRenderer` | `property compile` | `CallbackType` | `(scene: Object3D, camera: Camera, targetScene?: Scene | null) => Set<Material>` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebGLRenderer` | `property compileAsync` | `CallbackType` | `(scene: Object3D, camera: Camera, targetScene?: Scene | null) => Promise<Object3D>` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebGLRenderer` | `method getContext` | `UnmappedUnion` | return type: `WebGLRenderingContext | WebGL2RenderingContext` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `WebGLRenderer` | `method getContextAttributes` | `DomOrLibType` | return type: `WebGLContextAttributes` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `WebGLRenderer` | `method getSize` | `MathValueType` | parameter 'target': `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `WebGLRenderer` | `method getDrawingBufferSize` | `MathValueType` | parameter 'target': `Vector2` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `WebGLRenderer` | `method setEffects` | `CollectionType` | parameter 'effects': `Effect[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `WebGLRenderer` | `method getCurrentViewport` | `MathValueType` | parameter 'target': `Vector4` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `WebGLRenderer` | `method getViewport` | `MathValueType` | parameter 'target': `Vector4` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `WebGLRenderer` | `method setViewport` | `UnmappedUnion` | parameter 'x': `Vector4 | number` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `WebGLRenderer` | `method getScissor` | `MathValueType` | parameter 'target': `Vector4` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `WebGLRenderer` | `method setScissor` | `UnmappedUnion` | parameter 'x': `Vector4 | number` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `WebGLRenderer` | `method setOpaqueSort` | `CallbackType` | parameter 'method': `(a: any, b: any) => number` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebGLRenderer` | `method setTransparentSort` | `CallbackType` | parameter 'method': `(a: any, b: any) => number` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebGLRenderer` | `method setNodesHandler` | `OptionsInterface` | parameter 'nodesHandler': `NodesHandler` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `WebGLRenderer` | `method renderBufferDirect` | `OptionsInterface` | parameter 'group': `GeometryGroup` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `WebGLRenderer` | `method setAnimationLoop` | `ExternalType` | parameter 'callback': `XRFrameRequestCallback` is declared in another package |
| `WebGLRenderer` | `method setRenderTarget` | `UnmappedUnion` | parameter 'renderTarget': `WebGLRenderTarget | WebGLRenderTarget<Texture[]> | null` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `WebGLRenderer` | `method readRenderTargetPixels` | `UnmappedUnion` | parameter 'renderTarget': `WebGLRenderTarget | WebGLRenderTarget<Texture[]>` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `WebGLRenderer` | `method readRenderTargetPixelsAsync` | `UnmappedUnion` | parameter 'renderTarget': `WebGLRenderTarget | WebGLRenderTarget<Texture[]>` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `WebGLRenderer` | `method copyFramebufferToTexture` | `ConstructorOverloads` | parameter 'texture': `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `WebGLRenderer` | `method copyTextureToTexture` | `ConstructorOverloads` | parameter 'srcTexture': `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `WebGLRenderer` | `method initTexture` | `ConstructorOverloads` | parameter 'texture': `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `WebGLShadowMap` | `method render` | `CollectionType` | parameter 'shadowsArray': `Light[]` is an array, and `ThreeValue.Encode` has no array arm — an array argument has no wire encoding |
| `WebGLState` | `property buffers` | `AnonymousObjectType` | `{ color: WebGLColorBuffer; depth: WebGLDepthBuffer; stencil: WebGLStencilBuffer; }` is an anonymous object literal type with no named C# equivalent |
| `WebGLState` | `method bindFramebuffer` | `DomOrLibType` | parameter 'framebuffer': `WebGLFramebuffer` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `WebGLState` | `method drawBuffers` | `DomOrLibType` | parameter 'framebuffer': `WebGLFramebuffer` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `WebGLState` | `method useProgram` | `DomOrLibType` | parameter 'program': `WebGLProgram` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `WebGLState` | `method setBlending` | `UnmappedTypeAlias` | parameter 'blendSrc': `BlendingSrcFactor` aliases `BlendingDstFactor | typeof SrcAlphaSaturateFactor`, which is neither a group of numeric constants nor a type the mirror expresses |
| `WebGLState` | `method bindTexture` | `DomOrLibType` | parameter 'webglTexture': `WebGLTexture` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `WebGLState` | `method pixelStorei` | `DomOrLibType` | parameter 'name': `GLenum` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `WebGLState` | `method getParameter` | `DomOrLibType` | parameter 'name': `GLenum` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `WebGLState` | `method compressedTexImage2D` | `DomOrLibType` | parameter 'target': `GLenum` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `WebGLState` | `method compressedTexImage3D` | `DomOrLibType` | parameter 'target': `GLenum` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `WebGLState` | `method texSubImage2D` | `DomOrLibType` | parameter 'target': `GLenum` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `WebGLState` | `method texSubImage3D` | `DomOrLibType` | parameter 'target': `GLenum` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `WebGLState` | `method compressedTexSubImage2D` | `DomOrLibType` | parameter 'target': `GLenum` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `WebGLState` | `method compressedTexSubImage3D` | `DomOrLibType` | parameter 'target': `GLenum` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `WebGLState` | `method texStorage2D` | `DomOrLibType` | parameter 'target': `GLenum` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `WebGLState` | `method texStorage3D` | `DomOrLibType` | parameter 'target': `GLenum` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `WebGLState` | `method texImage2D` | `DomOrLibType` | parameter 'target': `GLenum` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `WebGLState` | `method texImage3D` | `DomOrLibType` | parameter 'target': `GLenum` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `WebGLState` | `method scissor` | `MathValueType` | parameter 'scissor': `Vector4` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `WebGLState` | `method viewport` | `MathValueType` | parameter 'viewport': `Vector4` is not an emitted class: a `src/math/**` value type. The mirror represents math values by value, encoded inline on the wire, not as handle-backed objects. Five are hand-written (`Color`, `Euler`, `Matrix4`, `Quaternion`, `Vector3`) and are never regenerated; giving the rest a representation is a public-API decision, not a mapping one |
| `WebGLTextures` | `method setTexture2D` | `ConstructorOverloads` | parameter 'texture': `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `WebGLTextures` | `method setTexture2DArray` | `ConstructorOverloads` | parameter 'texture': `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `WebGLTextures` | `method setTexture3D` | `ConstructorOverloads` | parameter 'texture': `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `WebGLTextures` | `method setTextureCube` | `ConstructorOverloads` | parameter 'texture': `Texture` is not an emitted class: 2 constructor overloads; C# overload emission is not implemented |
| `WebGLUtils` | `method convert` | `UnmappedUnion` | parameter 'p': `PixelFormat | CompressedPixelFormat | TextureDataType` unions 3 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `WebGPUBackend` | `property isWebGPUBackend` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `WebGPUBackend` | `property coordinateSystem` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `CoordinateSystem`), and the wire format has no read op — C# could neither write it nor observe it |
| `WebGPURenderer` | `property isWebGPURenderer` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `WebGPURenderer` | `property isWebGPURenderer` | `LiteralType` | `true` is a literal type — three.js's `isMesh`-style runtime type tag — which C# has no equivalent for outside an enum member |
| `WebXRController` | `method getHandSpace` | `UnwrappedClass` | return type: `XRHandSpace` is not an emitted class: renderer internals under `src/renderers/webxr/**`; no consumer instantiates them and emitting them would inflate the coverage table |
| `WebXRController` | `method getTargetRaySpace` | `UnwrappedClass` | return type: `XRTargetRaySpace` is not an emitted class: renderer internals under `src/renderers/webxr/**`; no consumer instantiates them and emitting them would inflate the coverage table |
| `WebXRController` | `method getGripSpace` | `UnwrappedClass` | return type: `XRGripSpace` is not an emitted class: renderer internals under `src/renderers/webxr/**`; no consumer instantiates them and emitting them would inflate the coverage table |
| `WebXRController` | `method dispatchEvent` | `AnonymousObjectType` | parameter 'event': `{ type: XRControllerEventType; data?: XRInputSource }` is an anonymous object literal type with no named C# equivalent |
| `WebXRController` | `method connect` | `ExternalType` | parameter 'inputSource': `XRInputSource` is declared in another package |
| `WebXRController` | `method disconnect` | `ExternalType` | parameter 'inputSource': `XRInputSource` is declared in another package |
| `WebXRController` | `method update` | `ExternalType` | parameter 'inputSource': `XRInputSource` is declared in another package |
| `WebXRDepthSensing` | `method init` | `ExternalType` | parameter 'depthData': `XRWebGLDepthInformation` is declared in another package |
| `WebXRDepthSensing` | `method getMesh` | `UnmappedTypeAlias` | parameter 'cameraXR': `WebXRArrayCamera` aliases `Omit<ArrayCamera, "cameras"> & { cameras: [WebXRCamera, WebXRCamera] }`, which is neither a group of numeric constants nor a type the mirror expresses |
| `WebXRManager` | `property getController` | `CallbackType` | `(index: number) => XRTargetRaySpace` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebXRManager` | `property getControllerGrip` | `CallbackType` | `(index: number) => XRGripSpace` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebXRManager` | `property getHand` | `CallbackType` | `(index: number) => XRHandSpace` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebXRManager` | `property setFramebufferScaleFactor` | `CallbackType` | `(value: number) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebXRManager` | `property setReferenceSpaceType` | `CallbackType` | `(value: XRReferenceSpaceType) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebXRManager` | `property getReferenceSpace` | `CallbackType` | `() => XRReferenceSpace | null` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebXRManager` | `property setReferenceSpace` | `CallbackType` | `(value: XRReferenceSpace) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebXRManager` | `property getBaseLayer` | `CallbackType` | `() => XRWebGLLayer | XRProjectionLayer` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebXRManager` | `property getBinding` | `CallbackType` | `() => XRWebGLBinding` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebXRManager` | `property getFrame` | `CallbackType` | `() => XRFrame` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebXRManager` | `property getSession` | `CallbackType` | `() => XRSession | null` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebXRManager` | `property setSession` | `CallbackType` | `(value: XRSession | null) => Promise<void>` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebXRManager` | `property getEnvironmentBlendMode` | `CallbackType` | `() => XREnvironmentBlendMode | undefined` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebXRManager` | `property getDepthTexture` | `CallbackType` | `() => ExternalTexture | null` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebXRManager` | `property updateCamera` | `CallbackType` | `(camera: PerspectiveCamera) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebXRManager` | `property getCamera` | `CallbackType` | `() => WebXRArrayCamera` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebXRManager` | `property getFoveation` | `CallbackType` | `() => number | undefined` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebXRManager` | `property setFoveation` | `CallbackType` | `(value: number) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebXRManager` | `property hasDepthSensing` | `CallbackType` | `() => boolean` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebXRManager` | `property getDepthSensingMesh` | `CallbackType` | `() => Mesh | null` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebXRManager` | `property getCameraTexture` | `CallbackType` | `(xrCamera: WebXRCamera) => ExternalTexture | undefined` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebXRManager` | `property setAnimationLoop` | `CallbackType` | `(callback: XRFrameRequestCallback | null) => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WebXRManager` | `property dispose` | `CallbackType` | `() => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `WireframeGeometry` | `property type` | `UnmappedUnion` | `string | "WireframeGeometry"` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `WireframeGeometry` | `property parameters` | `AnonymousObjectType` | `{ readonly geometry: TBufferGeometry; }` is an anonymous object literal type with no named C# equivalent |
| `XRHandSpace` | `property joints` | `DomOrLibType` | `Partial<XRHandJoints>` is a TypeScript lib type; C# holds no browser object and the wire format has no encoding for one |
| `XRHandSpace` | `property inputState` | `OptionsInterface` | `XRHandInputState` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `XRJointSpace` | `property jointRadius` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `number | undefined`), and the wire format has no read op — C# could neither write it nor observe it |
| `XRManager` | `property isPresenting` | `ReadOnlyWithoutReadChannel` | read-only in three.js (declared `boolean`), and the wire format has no read op — C# could neither write it nor observe it |
| `XRManager` | `method getController` | `UnwrappedClass` | return type: `XRTargetRaySpace` is not an emitted class: renderer internals under `src/renderers/webxr/**`; no consumer instantiates them and emitting them would inflate the coverage table |
| `XRManager` | `method getControllerGrip` | `UnwrappedClass` | return type: `XRGripSpace` is not an emitted class: renderer internals under `src/renderers/webxr/**`; no consumer instantiates them and emitting them would inflate the coverage table |
| `XRManager` | `method getHand` | `UnwrappedClass` | return type: `XRHandSpace` is not an emitted class: renderer internals under `src/renderers/webxr/**`; no consumer instantiates them and emitting them would inflate the coverage table |
| `XRManager` | `method getReferenceSpaceType` | `ExternalType` | return type: `XRReferenceSpaceType` is declared in another package |
| `XRManager` | `method setReferenceSpaceType` | `ExternalType` | parameter 'type': `XRReferenceSpaceType` is declared in another package |
| `XRManager` | `method getReferenceSpace` | `ExternalType` | return type: `XRReferenceSpace` is declared in another package |
| `XRManager` | `method setReferenceSpace` | `ExternalType` | parameter 'space': `XRReferenceSpace` is declared in another package |
| `XRManager` | `method getEnvironmentBlendMode` | `ExternalType` | return type: `XREnvironmentBlendMode` is declared in another package |
| `XRManager` | `method getBaseLayer` | `UnmappedUnion` | return type: `XRWebGLLayer | XRProjectionLayer` unions 2 distinct types; C# cannot express that as one parameter and picking one arm would narrow the API silently |
| `XRManager` | `method getBinding` | `ExternalType` | return type: `XRWebGLBinding` is declared in another package |
| `XRManager` | `method getWebGPUBinding` | `OptionsInterface` | return type: `XRGPUBinding` is an interface, and the mirror has no representation for a structural type — only for classes it constructs by handle |
| `XRManager` | `method getFrame` | `ExternalType` | return type: `XRFrame` is declared in another package |
| `XRManager` | `method createQuadLayer` | `CallbackType` | parameter 'rendercall': `() => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `XRManager` | `method createCylinderLayer` | `CallbackType` | parameter 'rendercall': `() => void` is a JavaScript callback, and the wire format carries ops in one direction only — there is no channel to call back into C# |
| `XRManager` | `method getSession` | `ExternalType` | return type: `XRSession` is declared in another package |
| `XRManager` | `method setSession` | `ExternalType` | parameter 'session': `XRSession` is declared in another package |

</details>

## Enums

three.js closes a value set in two ways, and both become one C# enum:

- a real TypeScript `enum` — a direct translation, nothing is inferred;
- loose `export const`s **grouped by a type alias** that unions `typeof` of each one. The grouping is
  three.js's own (`type Side = typeof FrontSide | typeof BackSide | typeof DoubleSide`), read out of the
  declaration rather than guessed from a name prefix. A constant no alias groups stays a constant: an
  invented grouping would be worse than none.

A group is only generatable when every value is **numeric**, because the wire encoder sends a C# enum
as its numeric backing value — a string-valued group would arrive as a number where three.js expects
the string. The backing type is the narrowest that holds every value, so three.js's small flag sets stay
`byte` and its WebGL constants land on `ushort`.

**33 generated**: 30 inferred from a constant group, 3 from a real TypeScript `enum`.
22 are referenced by a mapped member today; the rest are emitted anyway, because an enum is a
leaf type whose availability should not move with the class surface.

| enum | source | members | aliases | backing type | referenced |
|---|---|---|---|---|---|
| `AnimationActionLoopStyles` | constant group | 3 | 0 | `ushort` | yes |
| `AnimationBlendMode` | constant group | 2 | 0 | `ushort` | yes |
| `AttributeGPUType` | constant group | 2 | 0 | `ushort` | yes |
| `Blending` | constant group | 7 | 0 | `byte` | yes |
| `BlendingDstFactor` | constant group | 14 | 0 | `byte` | yes |
| `BlendingEquation` | constant group | 5 | 0 | `byte` | yes |
| `Combine` | constant group | 3 | 0 | `byte` | no |
| `CoordinateSystem` | constant group | 2 | 0 | `ushort` | yes |
| `CubeTextureMapping` | constant group | 3 | 0 | `ushort` | yes |
| `CullFace` | constant group | 4 | 0 | `byte` | yes |
| `DepthModes` | constant group | 8 | 0 | `byte` | yes |
| `DepthPackingStrategies` | constant group | 4 | 0 | `ushort` | no |
| `DepthTexturePixelFormat` | constant group | 2 | 0 | `ushort` | yes |
| `GPUColorWriteFlags` | TypeScript `enum` | 6 | 0 | `byte` | no |
| `InterpolationEndingModes` | constant group | 3 | 0 | `ushort` | no |
| `InterpolationModes` | constant group | 4 | 0 | `ushort` | yes |
| `MOUSE` | TypeScript `enum` | 6 | 3 | `byte` | no |
| `MagnificationTextureFilter` | constant group | 2 | 0 | `ushort` | yes |
| `Mapping` | constant group | 3 | 0 | `ushort` | yes |
| `MinificationTextureFilter` | constant group | 10 | 4 | `ushort` | yes |
| `NormalMapTypes` | constant group | 2 | 0 | `byte` | no |
| `PixelFormat` | constant group | 11 | 0 | `ushort` | yes |
| `ShadowMapType` | constant group | 4 | 0 | `byte` | yes |
| `Side` | constant group | 3 | 0 | `byte` | no |
| `StencilFunc` | constant group | 8 | 0 | `ushort` | no |
| `StencilOp` | constant group | 8 | 0 | `ushort` | no |
| `TOUCH` | TypeScript `enum` | 4 | 0 | `byte` | no |
| `TextureComparisonFunction` | constant group | 8 | 0 | `ushort` | yes |
| `TextureDataType` | constant group | 13 | 0 | `ushort` | yes |
| `ToneMapping` | constant group | 8 | 0 | `byte` | yes |
| `TrianglesDrawModes` | constant group | 3 | 0 | `byte` | no |
| `Usage` | constant group | 9 | 0 | `ushort` | yes |
| `Wrapping` | constant group | 3 | 0 | `ushort` | yes |

### Duplicate values

three.js gives several members the same number — `MOUSE.LEFT` and `MOUSE.ROTATE` are both `0`, and
`MinificationTextureFilter` carries four deprecated `MipMap` spellings of its `Mipmap` values. C# rejects
two members declared with the same literal, so the second names the first instead. No member is dropped:

- `MOUSE`: `ROTATE` = `LEFT`, `DOLLY` = `MIDDLE`, `PAN` = `RIGHT`
- `MinificationTextureFilter`: `NearestMipMapNearestFilter` = `NearestMipmapNearestFilter`, `NearestMipMapLinearFilter` = `NearestMipmapLinearFilter`, `LinearMipMapNearestFilter` = `LinearMipmapNearestFilter`, `LinearMipMapLinearFilter` = `LinearMipmapLinearFilter`

### Constants left ungrouped

23 of the 226 exported constants belong to no alias, so no enum claims them. Grouping them
would mean inventing a set three.js never declared, and an enum is a promise that its values are
exhaustive and mutually exclusive — a promise only the upstream alias is in a position to make.

Most are not value sets at all: only 4 of the 23 are a literal. The rest are namespace objects
(`MathUtils`, `ShaderChunk`, `UniformsLib`, `Cache`), which no grouping rule would have reached.

<details><summary>Every ungrouped constant (23)</summary>

| constant | value | file |
|---|---|---|
| `AttributeType` | not a literal — a `object` type | `src/renderers/common/Constants.d.ts` |
| `BlendColorFactor` | `211` | `src/renderers/common/Constants.d.ts` |
| `Cache` | not a literal — a `object` type | `src/loaders/Cache.d.ts` |
| `ColorManagement` | not a literal — a `reference` type | `src/math/ColorManagement.d.ts` |
| `Compatibility` | not a literal — a `object` type | `src/constants.d.ts` |
| `DEG2RAD` | not a literal — a `primitive` type | `src/math/MathUtils.d.ts` |
| `DefaultLoadingManager` | not a literal — a `reference` type | `src/loaders/LoadingManager.d.ts` |
| `GPU_CHUNK_BYTES` | `16` | `src/renderers/common/Constants.d.ts` |
| `InterpolationSamplingMode` | not a literal — a `object` type | `src/constants.d.ts` |
| `InterpolationSamplingType` | not a literal — a `object` type | `src/constants.d.ts` |
| `MathUtils` | not a literal — a `object` type | `src/math/MathUtils.d.ts` |
| `OneMinusBlendColorFactor` | `212` | `src/renderers/common/Constants.d.ts` |
| `RAD2DEG` | not a literal — a `primitive` type | `src/math/MathUtils.d.ts` |
| `REVISION` | not a literal — a `primitive` type | `src/constants.d.ts` |
| `ReversedDepthFuncs` | not a literal — a `mapped` type | `src/utils.d.ts` |
| `ShaderChunk` | not a literal — a `object` type | `src/renderers/shaders/ShaderChunk.d.ts` |
| `ShaderLib` | not a literal — a `object` type | `src/renderers/shaders/ShaderLib.d.ts` |
| `SrcAlphaSaturateFactor` | `210` | `src/constants.d.ts` |
| `TextureUtils` | not a literal — a `object` type | `src/extras/TextureUtils.d.ts` |
| `TimestampQuery` | not a literal — a `object` type | `src/constants.d.ts` |
| `UniformsLib` | not a literal — a `object` type | `src/renderers/shaders/UniformsLib.d.ts` |
| `UniformsUtils` | not a literal — a `object` type | `src/renderers/shaders/UniformsUtils.d.ts` |
| `_colorKeywords` | not a literal — a `object` type | `src/math/Color.d.ts` |

</details>

### Refused

| name | why |
|---|---|
| `BindMode` | the group is string-valued (`AttachedBindMode` = "attached"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `ColorSpace` | the group is string-valued (`NoColorSpace` = ""); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `ColorSpaceTransfer` | the group is string-valued (`LinearTransfer` = "linear"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `CompressedPixelFormat` | constant `RGB_BPTC_SIGNED_Format` has no literal value in the IR (declared type `<none>`) |
| `GLSLVersion` | the group is string-valued (`GLSL1` = "100"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPUAddressMode` | the enum is string-valued (`ClampToEdge` = "clamp-to-edge"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPUBlendFactor` | the enum is string-valued (`Zero` = "zero"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPUBlendOperation` | the enum is string-valued (`Add` = "add"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPUBufferBindingType` | the enum is string-valued (`Uniform` = "uniform"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPUCompareFunction` | the enum is string-valued (`Never` = "never"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPUCullMode` | the enum is string-valued (`None` = "none"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPUFeatureMap` | the enum is string-valued (`texture-compression-s3tc` = "texture-compression-bc"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPUFeatureName` | the enum is string-valued (`CoreFeaturesAndLimits` = "core-features-and-limits"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPUFilterMode` | the enum is string-valued (`Linear` = "linear"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPUFrontFace` | the enum is string-valued (`CCW` = "ccw"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPUIndexFormat` | the enum is string-valued (`Uint16` = "uint16"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPUInputStepMode` | the enum is string-valued (`Vertex` = "vertex"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPULoadOp` | the enum is string-valued (`Load` = "load"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPUPrimitiveTopology` | the enum is string-valued (`PointList` = "point-list"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPUSamplerBindingType` | the enum is string-valued (`Filtering` = "filtering"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPUStencilOperation` | the enum is string-valued (`Keep` = "keep"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPUStorageTextureAccess` | the enum is string-valued (`WriteOnly` = "write-only"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPUStoreOp` | the enum is string-valued (`Store` = "store"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPUTextureAspect` | the enum is string-valued (`All` = "all"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPUTextureDimension` | the enum is string-valued (`OneD` = "1d"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPUTextureFormat` | the enum is string-valued (`R8Unorm` = "r8unorm"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPUTextureSampleType` | the enum is string-valued (`Float` = "float"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPUTextureViewDimension` | the enum is string-valued (`OneD` = "1d"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `GPUVertexFormat` | the enum is string-valued (`Uint8x2` = "uint8x2"); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |
| `NormalPacking` | the group is string-valued (`NoNormalPacking` = ""); a C# enum is sent over the wire as its numeric backing value, so it would arrive as a number where three.js expects the string |

## Unspecified arguments in a middle position

An optional parameter whose three.js default the types do not state is emitted as `T? x = null`,
meaning "not supplied". `ConstructorArgs` trims the unsupplied **tail** off the argument list, so
three.js applies its own default to it.

Trimming only reaches the end. A JSON `null` is not JavaScript's `undefined`: `function f(a = 1) {}`
called as `f(null)` yields `null`, not `1`. So an unspecified parameter with a supplied one after it
cannot be trimmed and must not be sent as null either — it travels as the `$undef` sentinel
(`ThreeWireFormat.UndefinedKey`), which `three-interop.js` decodes to a real `undefined`. The
round trip is pinned end to end by `tests/wire-format.test.mjs` against the vendored three.js.

27 emittable classes carry 59 such parameters. They are the measure of how much
of the emitted surface that one wire feature holds up.

<details><summary>Every affected class</summary>

| class | parameters that depend on the `$undef` sentinel |
|---|---|
| `AmbientLight` | `color` |
| `AnimationClip` | `name` |
| `ArcCurve` | `aRadius`, `aEndAngle` |
| `ArrowHelper` | `dir`, `origin`, `color`, `headLength` |
| `BatchedMesh` | `maxIndexCount` |
| `CubeDepthTexture` | `type`, `mapping`, `wrapS`, `wrapT`, `magFilter`, `minFilter`, `anisotropy` |
| `CubicBezierCurve3` | `v0`, `v1`, `v2` |
| `DepthTexture` | `width`, `height`, `type`, `mapping`, `wrapS`, `wrapT`, `magFilter`, `minFilter`, `anisotropy`, `format` |
| `DirectionalLight` | `color` |
| `EdgesGeometry` | `geometry` |
| `EllipseCurve` | `aEndAngle` |
| `GridHelper` | `color1` |
| `HemisphereLight` | `skyColor`, `groundColor` |
| `LineCurve3` | `v1` |
| `PointLight` | `color` |
| `PolarGridHelper` | `color1` |
| `QuadraticBezierCurve3` | `v0`, `v1` |
| `Raycaster` | `origin`, `direction` |
| `RectAreaLight` | `color` |
| `RenderTarget` | `width` |
| `RenderTarget3D` | `width`, `height` |
| `SphereGeometry` | `phiLength` |
| `SpotLight` | `color`, `angle` |
| `StorageTexture` | `width` |
| `TorusGeometry` | `arc` |
| `VideoFrameTexture` | `mapping`, `wrapS`, `wrapT`, `magFilter`, `minFilter`, `format`, `type` |
| `WebGLRenderTarget` | `width` |

</details>

